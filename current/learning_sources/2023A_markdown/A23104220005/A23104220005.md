# A23104220005

> Source: `A23104220005.pdf`  
> Pages: 49  
> Text is extracted to Markdown. Exact page renderings are stored as JPEG fallback files and linked (not embedded) to avoid automatic image parsing.


<!-- page: 1 -->

学   校 山东大学参赛队号 23104220005
## 1. 汪健

队员姓名 2. 李昌盛
## 3. 高一凡


> Visual fallback for page 1: [page_001.jpg](page_images/page_001.jpg)


<!-- page: 2 -->

题   目          WLAN 网络信道接入机制建模
## 摘 要：

随着互联网的飞速发展，无线局域网 WLAN 因其特有的移动性和灵活性，受到了学术界与工业界的高度关注，现阶段广泛使用的 WLAN 是基于 IEEE 802.11 标准实现，其媒体访问控制 MAC 层的协议是为了有效的共享有限的信道资源，因此需要对 MAC 层协议进行深入研究，通过数学模型的建立与仿真实验分析可以有效地分析出无线局域网的性能。

本文主要研究了分布式协调功能 DCF，根据 CSMA/CA 与二进制指数退避算法，建立马尔科夫链模型，推导不同场景中节点的多种状态以及在不同状态下达到稳态时各状态的转移概率，计算出系统的理论吞吐量。本文建立的模型是基于 Bianchi 等人提出的经典的马尔科夫链进行改良，以更好地适应现实的复杂无线传输场景。

针对问题一，考虑两 BSS 互听且两 AP 并发时 SIR 较小的场景，将节点退避计数器的状态变化建模为二维时间离散状态离散马尔科夫过程，通过理论分析和数学推导得到系统的稳态表达式，并通过牛顿迭代法求解二元非线性方程组得到系统的吞吐量为 67.17Mbps，通过与仿真结果进行对比得到模型误差为 0.45%。

针对问题二，考虑两 BSS 互听且两 AP 并发时 SIR 较大的场景，将节点退避计数器的状态变化建模为一维时间离散状态离散马尔科夫过程，间接反映出两 AP 在信道接入过程中的相互作用，通过该模型得到系统吞吐量为 70.56Mbps，与仿真结果进行比较得到模型误差为 3.6%。

针对问题三，考虑两 BSS 不互听，并发时 SIR 较小且信道质量不理想的场景，将丢包概率融入到二维时间离散状态离散马尔科夫过程中，反映出信道质量和隐藏节点对系统吞吐的共同影响，通过牛顿迭代法得到系统吞吐为 49.18Mbps，与仿真结果进行比较得到模型误差据 4.57%。

针对问题四，考虑三 BSS 且并发时 SIR 较大的复杂场景，分析不同 AP 之间的相互作用的差异，并将这一差异反映到二维时间离散状态离散马尔科夫过程中，通过对该随机过程进行理论推导，并通过牛顿迭代法进行求解得到系统吞吐量为 106.54Mbps，与仿真结果进行比较得到模型误差为 11.69%。

关键词： 无线局域网；分布式协调功能 DCF；马尔可夫过程；系统吞吐量；牛顿迭代法

> Visual fallback for page 2: [page_002.jpg](page_images/page_002.jpg)


<!-- page: 3 -->

## 目录

1. 问题重述 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .        3 1 . 1 背景介绍 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .             3 1 . 2 问题提出 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .             4 2. 问题分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .        6 2 . 1 问题一分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .                6 2 . 2 问题二分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .                6 2 . 3 问题三分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .                7 2 . 4 问题四分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .                7 3. 模型的假设 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .           8 4. 符号说明与缩略语对照 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 9 4 . 1 符号说明 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 9 4 . 2 缩略语对照 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 5. 模型的建立及求解 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 5 . 1 问题一 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 5 . 1 . 1 模型建立与求解 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 5 . 1 . 2 仿真实验分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14 5 . 1 . 3 结果分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 5 . 2 问题二 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 16 5 . 2 . 1 模型建立与求解 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 16 5 . 2 . 2 仿真实验分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 18 5 . 2 . 3 结果分析 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 5 . 3 问题三 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 5 . 3 . 1 模型建立与求解 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 5 . 3 . 2 仿真实验 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 25 5 . 4 问题四 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 5 . 4 . 1 模型建立与求解 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 5 . 4 . 2 仿真实验 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 6. 模型评价 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 35 6 . 1 模型的优点 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 35 6 . 2 模型的缺点 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 35参考文献 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 36附录 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 37

> Visual fallback for page 3: [page_003.jpg](page_images/page_003.jpg)


<!-- page: 4 -->

## 1. 问题重述

## 1 . 1 背景介绍

随着无线局域网络 WLAN (wireless local area networks) 的快速发展，用户无需有线连接即可随时访问互联网，这极大提高工作效率和生产力。无线局域网有着实现相对简单、部署方便、使用灵活、组网便捷、实现成本相对较低、传输速率相对较高等诸多优点。伴随着手机，平板电脑，智能家居设备等无线终端设备的爆发式增长，WLAN 已在家庭、学校、办公室、商务酒店等场所得到广泛使用。WLAN 已成为目前最流行的无线终端设备接入互联网方式之一。

作为新型的无线通信方式，WLAN 在许多领域发挥着自己独特的作用：（1）一些情况比较复杂的场地，通常难以使用传统有线网络建立连接，可以使用 WLAN 解决布线难题。（2）在展览会场、大型体育场馆和救灾现场等临时性场所，可以使用 WLAN 实现更加便捷有效的网络通信。（3）在移动通信环境下，移动端用户可以通过 WLAN 无缝地连接和切换到不同的节点来请求信息访问和数据传输。

WLAN 通常指采用无线工作方式，空中接口采用载波侦听多路访问碰撞避免技术实现共享媒质接入控制的一种局域网技术。基本服务集（BSS, basic service set）是 WLAN 基本的组成部分。一个 BSS 通常由站点（STA, station）与无线接入点（AP, access point）构成。

由于无线网络的特性，一个节点无法同时发送与接收数据，其中的节点既是指 AP，也是指 STA。AP 是无线网络的中心节点，可以与有线网络建立连接，所有 STA 的无线通信均需 AP 完成，一个 BSS 的基础网络结构如图 1.1 所示。

图 1.1   BSS 基础结构网络

作为 WLAN 协议的重要组成部分，WLAN 网络信道接入机制一直是无线通信领域的热门研究方向。与有线网络连接方式相比，WLAN 采用多节点间共享无线信道进行数据传输，同时这带来了传输碰撞、相互干扰（同频或异频干扰）                              、隐藏节点等问题。因此，我们需要对 WLAN 网络信道接入机制进行分析与建模，分析系统的网络吞吐量和网络传输时延，以改善 WLAN 的性能。

现阶段广泛使用的 WLAN 是基于 IEEE 802.11 标准实现，该标准详细定义了两种工作方式，分别是分布式协调功能（DCF, distributed coordination function）和点协调功能（PCF,point coordination function），DCF 功能的基础是载波监听多址接入/冲突避免（CSMA/CA,carrier sense multi-access and collision avoidance）协议，现阶段运营商实际部署 WLAN 使用的网络设备均支持 DCF 工作方式。在使用理论模型对 DCF 功能进行数学分析时，通常采用马尔科夫链模型。马尔可夫过程是一类常见的无后效的随机过程，即下一时刻所处状态的概率特性只与当前时刻的状态有关。Bianchi 根据其特性建模出适合 IEEE 802.11 协议的基于二维离散马尔科夫链的吞吐量模型，通过 Bianchi 模型可以很好地分析出饱和状态下WLAN 性能，展示出较好的参考价值，成为后来学者研究改进的基础。

> Visual fallback for page 4: [page_004.jpg](page_images/page_004.jpg)


<!-- page: 5 -->

## 1 . 2 问题提出

本题首先介绍单 BSS 系统内节点接入信道的具体机制，即包含信道可用评估（CCA，clear channel assessment）、随机回退、数据传输 3 个阶段的分布式信道接入机制，其中采用二进制指数退避算法确定回退时间。然后基于马尔可夫链对系统的 DCF 机制进行建模，推导得出了系统的饱和状态吞吐量与冲突概率之间的关系表达式。这一经典的 Bianchi 模型是在假设理想信道分析 DCF 机制的系统状态，不存在隐藏终端，也不存在数据包丢失的情况。由于过于简化网络环境，Bianchi 模型存在着一些明显的不足之处。

在多 BSS 场景下，场景更为复杂，不仅需要考虑多个 AP 部署场景下的相互干扰问题，也需要考虑节点间能否互听问题与多节点并发传输问题，可能无法直接地用于这些复杂场景。例如，      （1）多节点并发传输时，如果 SIR 足够高使得信号能被成功解调，并发传输均成功，Bianchi 模型没有考虑来自不同 AP 的并发传输，其的适用性受到了限制。          （2）隐藏终端的存在会导致多节点间发送碰撞概率的变动，Bianchi 模型不在适用。           （3）在实际应用中，信道环境会随着环境干扰发生比较大的变化，导致不同程度的丢包，Bianchi 模型亦无法适用。因此，需要根据问题场景的不同，分别对 Bianchi 模型进行改进，推导特定环境下节点的发送概率与碰撞概率，得到该场景下系统吞吐量。

图 1.2   2 同频 BSS 场景

问题一：考虑如图 1.2 所示的两个同频 BSS 互听的场景，AP 节点仅进行下行传输；模型假设理想信道，数据发送不会因信道质量差而丢包，传输错误仅仅由于传输碰撞造成；

ACK 一定能发送成功；当两个 AP 同时回退到 0，会分别向其关联的 STA 发送数据，此时存在相互干扰，因并发时的 SIR 较低，两个 AP 节点数据传输均失败。通过分析信道存在的三种状态包括空闲、成功和失败，考虑系统状态间的相互转化，将每个状态看做一个虚拟时隙，信道状态在三种虚拟时隙中转化。基于马尔科夫链进行 2BSS 系统建模，采用数值分析根据给定的网络参数进行计算，推导节点在每个虚拟时隙的发送概率 τ 和发生碰撞的条件概率 p，进而得出系统吞吐理论值。使用仿真工具进行仿真实验，完成 WLAN 吞吐量建模测试，比较理论数值与仿真数值，分析验证模型正确性。

问题二：同样考虑如图 1.2 所示的两个同频 BSS 互听的场景，AP 节点仅进行下行传输；数据发送不会因信道质量差而丢包，ACK 一定能发送成功；当两个 AP 同时回退到 0，会分别向其关联的 STA 发送数据，存在相互干扰，但因并发传输时的 SIR 较低，两个 AP节点数据的传输都能成功，无需考虑竞争窗口的变化与最大重传次数的限制。基于马尔可夫链进行 2BSS 系统建模，带入给定的系统参数，采用数值分析方法计算求解，推导节点在每个虚拟时隙的发送概率 τ 和发生并行传输的条件概率 p，得出 2BSS 系统吞吐理论值，编写仿真验证完成 WLAN 吞吐量建模测试，比较理论数值与仿真数值，分析验证模型正确性。

问题三：考虑如图 1.2 所示的两个同频 BSS 场景，AP 节点仅考虑下行传输，两个发送节点无法互听，数据发送因信道质量差而产生 10 % 的丢包率，隐藏节点存在必须考虑当两个 AP 发包时间上存在交叠时的干扰，因 SIR 较小传输失败，采用数值分析根据给定的网络参数进行计算，推导节点在每个虚拟时隙发生碰撞的条件概率 p，节点在一个时隙发送数据帧的概率 τ1 ，和隐藏节点在易受干扰阶段发送数据与发送的数据冲突的概率 τ2 ，从

> Visual fallback for page 5: [page_005.jpg](page_images/page_005.jpg)


<!-- page: 6 -->

而可以计算出得出 2BSS 系统吞吐理论值。编写仿真验证完成 WLAN 吞吐量建模测试，比较理论数值与仿真数值，分析验证模型正确性。

问题四：考虑如图 1.3 所示的三个同频 BSS 场景，AP 节点仅考虑下行传输，AP1 与AP3 不互听，AP2 与 AP1、AP3 都互听，隐藏节点存在，必须考虑 AP1 与 AP3 发包时间上存在交叠时的干扰，因 SIR 较大发包交叠时 AP1 与 AP3 传输成功。AP2 与两者都互听，当发生碰撞时，假设 SIR 较小，认为两者数据传输都失败。通过分析信道存在的四种状态包括空闲、单节点传输、并行传输和传输失败。考虑系统状态间的相互转化，将每个状态看做一个虚拟时隙，信道状态在四种虚拟时隙中转化。基于马尔科夫链进行 3BSS 系统建模，采用数值分析根据给定的网络参数进行计算，推导节点在每个虚拟时隙的发送概率 τ和发生碰撞的条件概率 p，进而得出系统吞吐理论值。编写仿真验证完成 WLAN 吞吐量建模测试，比较理论数值与仿真数值，分析验证模型正确性。

图 1.3   3 同频 BSS 场景

> Visual fallback for page 6: [page_006.jpg](page_images/page_006.jpg)


<!-- page: 7 -->

## 2. 问题分析

## 2 . 1 问题一分析

本题中，两个 AP 采用共享的信道进行数据传输，假设每次 AP 成功传输数据后，都会有一个新的数据等待传输。给定 AP 发送包的载荷长度为 1500Bytes，MAC 头为 30Bytes，二者均以 455.8Mbps 的物理层速率传输。根据这些参数，我们分别可以计算出 MAC 头和有效载荷的发送时长。由于 DIFS，SIFS，ACK 时间与 ACKTimeout 时间为固定值，我们可以分别计算出成功传输和碰撞的传输时长 Ts 和 Tc 。要求出系统的吞吐量。我们需要计算出系统中的至少有一个节点发送数据的发送概率 Ptr ，有且只有一个节点正在传输的成功传输概率 Ps 。

本题中，2BSS 系统的发送概率 Ptr 与成功传输概率 Ps 与 AP 节点在每个虚拟时隙的发送概率 τ 和发生碰撞的条件概率 p 有关。由于系统中存在两个 BSS，并且二者互听，且存在同频干扰。将系统状态分为三种：空闲状态（准备进行数据传输的 AP 节点感知到该状态即可启动传输）    ，回退状态（系统内 AP 节点选择了一个后退计数器，并随着每个时隙的流逝而递减），传输状态（一个 AP 节点成功争用信道并积极传输数据帧）。考虑 AP 节点的退避器所处的阶数和随机回退数的变化（竞争窗口大小因数据发送失败进行重传而产生二进制指数增加至 CWmax ，保持此值，直至最大重传次数被重置为止），通过二维离散的马尔可夫链分析跟踪这三种状态之间的转换。建立方程组进行数学推导求解，可以得出AP 节点在每个虚拟时隙的发送概率率 τ 和发生碰撞的条件概率 p，进而计算出系统的发送概率 Ptr 与成功传输概率 Ps ，最后根据吞吐量关系表达式可以算出系统的吞吐量的理论值。

为了验证所建模型的正确性与合理性，我们选择使用 MATLAB 对本问题进行仿真场景模拟，将理论结果与仿真结果进行对比，分析并做出总结。
## 2 . 2 问题二分析

本题中，两个 AP 采用共享的信道进行数据传输，假设每次 AP 成功传输数据后，都会有一个新的数据等待传输。给定 AP 发送包和 MAC 头均以 275.3Mbps 的物理层速率传输，其余参数与问题一一致。根据这些给定的参数，我们分别可以计算出 MAC 头和有效载荷的发送时长。由于 DIFS，SIFS，ACK 时间与 ACKTimeout 时间为固定值，我们可以计算出成功传输和碰撞的传输时长 Ts 和 Tc 。要求出系统的吞吐量。我们需要计算出系统中的至少有一个节点发送数据的发送概率 Ptr ，有且只有一个节点正在传输的概率 Ps 。

本题中，2BSS 系统的成功传输概率与 AP 节点在每个虚拟时隙的发送概率 τ 和发生并行传输的条件概率 p 有关。由于系统中存在两个 BSS，并且二者互听，且存在同频干扰。

但因 SIR 足够高，两 AP 不会因碰撞而发送失败，两个数据的传输都能成功。改进 Bianchi模型，将二维的马尔科夫链降维成一维马尔可夫链，这是因为该系统中两个 AP 节点回退到 0 同时发送数据，因 SIR 足够高，两个数据的传输都能成功，无需考虑竞争窗口的变化与重传次数。将系统状态分为三种：空闲状态（准备进行数据传输的 AP 节点感知到该状态即可启动传输）   ，回退状态（系统内 AP 节点选择了一个后退计数器，并随着每个时隙的流逝而递减），传输状态（AP 节点成功争用信道并积极传输数据帧）。通过一维离散的马尔可夫链分析跟踪这三种状态之间的转换。建立方程组进行数学推导求解，可以得出 AP节点在每个虚拟时隙的发送概率 τ 和发生并行传输的条件概率 p，最后根据吞吐量关系表达式可以算出系统的吞吐量的理论值。

为了验证所建模型的正确性与合理性，我们选择使用 MATLAB 对本问题进行仿真场景模拟，将理论结果与仿真结果进行对比，分析并做出总结。

> Visual fallback for page 7: [page_007.jpg](page_images/page_007.jpg)


<!-- page: 8 -->

## 2 . 3 问题三分析

与问题 1 和问题 2 不同的是，本题 AP 间 RSSI 为-90dBm，低于 CCA 门限，意味着无法侦测对方的活动状态。AP 节点始终认为信道处于空闲状态，一直在退避和发送数据。当二者同时或者先后发送数据时，由于 SIR 值较小，数据发送均失败。由此可见，本题的关键在于求得两个 AP 节点在发送数据包时时间上存在交叠的概率。

本题中，若在开始发送到 STA 回复 ACK 消息之间，有其他节点发送数据，即认为传输时间存在交叠，数据传输失败。将这段时间定义为脆弱时间，长度为数据帧时间和 SIFS时间之和。当两 AP 节点发送时间之差小于脆弱时间，则一定发送失败；反之，考虑到有10% 的可能丢包，发送成功的概率仅为 90%。
## 2 . 4 问题四分析

同前三个问题相比，本题所考虑的情景更加复杂，在本题中，不但存在可以互听的 AP节点，而且也存在不互听的 AP 节点，具体表现为 AP1 与 AP3 不互听，AP2 与两者都互听，可以预知的是 AP2 的数据发送概率相比于其他两者要小，因为其他两者挤占了其数据发送机会。此外，该场景中 AP1 和 AP3 发生交叠时认为两者发送都成功，而当 AP2 与两者发生碰撞时数据传输都失败。基于以上分析，可以得知如何建立系统数学模型反映不同AP 节点间的数据发送概率和不同节点间的碰撞关系是能准确反应系统性能的前提。

本问题具体求解思路如下：对于系统中的任意一个节点，找到能与其互听的节点和不能与其互听的节点，并将他们分为两类，分别称为覆盖节点与隐藏节点；通过对其退避计数器的状态进行建模来求解节点的发送概率；由于隐藏节点的退避计数器计数不受影响，因此存在很大的概率出现两不互听节点先后发送数据的情况，因此通过节点发送数据包的大小和退避时隙的关系求出可能发生交叠时隐藏节点的发送概率；之后，得到互听节点间发生碰撞的概率，将以上求出的概率进行计算即可得到该系统的吞吐。

为了验证所建模型的正确性与合理性，我们选择使用 MATLAB 对本问题进行仿真场景模拟，将理论结果与仿真结果进行对比，分析并做出总结。

> Visual fallback for page 8: [page_008.jpg](page_images/page_008.jpg)


<!-- page: 9 -->

## 3. 模型的假设

• 本题 BSS 中，仅下行传输情况，即 AP 向其关联的 STA 传输数据；

• 本题仅存在同频干扰，不考虑异频干扰的情况；

• BSS 互听时，当有并发传输时，其成功与否与 SIR 有关，SIR 高，成功传输，否则传输失败；

• BSS 不互听时，有很大概率出现二者同时或先后开始发送数据的情况，信号包能否接收成功同样也由 SIR 决定，SIR 比较高可以成功传输。否则本次传输失败；

• 多 BSS 场景中，所有节点的数据帧长度相同；

• ACK 一定能发送成功；

• 本题中系统无环境噪声。

> Visual fallback for page 9: [page_009.jpg](page_images/page_009.jpg)


<!-- page: 10 -->

## 4. 符号说明与缩略语对照

## 4 . 1 符号说明

序号 符号                   意义
## 1 τ 节点在每个虚拟时隙的发送概率

## 2 p 节点在每个虚拟时隙的发生碰撞的条件概率

## 3 x(t) 一个 AP 退避随机过程的退避阶数

## 4 y(t) 一个 AP 退避随机过程的退避计数

## 5 t 一个离散的虚拟时隙的开始时刻

## 6 i 数据的发送次数

## 7 j 数据的回退计数

## 8 n 最大重传次数

## 9 m 最大退避阶数

## 10 Wi 第 i 次发送竞争窗大小

## 11 bi,j 二维马尔科夫链的稳态解

## 12 Ptr 至少有一个 AP 发送数据的概率

## 13 Ps 恰好仅有一个 AP 发送数据的概率

## 14 Te 空闲时隙的长度

## 15 Ts 成功传输的传输时长

## 16 Tc 碰撞的传输时长

## 17 bi 一维马尔科夫链的稳态解

## 18 Pe 每个虚拟时隙发生丢包的概率

## 19 P′ 每个虚拟时隙发包失败的概率

## 20 τ1 节点在一个时隙发送数据帧的概率

隐藏节点易受干扰时段期间发送数据
## 21 τ2

并与源发送的数据冲突的概率
## 22 V 易受干扰时段的长度

## 23 X 竞争窗口大小大于 V 的最小退避阶数


> Visual fallback for page 10: [page_010.jpg](page_images/page_010.jpg)


<!-- page: 11 -->

## 4 . 2 缩略语对照

序号      缩略语                   英文全称                      中文解释
## 1 AP access point 无线接入点

## 2 ACK Acknowledgement 确认

## 3 ACKTimeout 确认超时

## 4 BSS basic service set 基本服务集

## 5 CCA clear channel assessment 信道可用评估

## 6 CW contention window 竞争窗口

## 7 DCF distributed coordination function 分布式协调功能

## 8 DIFS DCF inter-frame space DCF 帧间距

## 9 MAC medium access control 媒体控制

## 10 PCF point coordination function 点协调功能

## 11 PHY physical 物理层

## 12 RSSI received signal strength indication 接收信号能量强度

## 13 SIFS short inter-frame space 短帧间距

## 14 SIR signal to interference ratio 信干比

## 15 STA station 站点

## 16 WLAN wireless local area network 无线局域网络


> Visual fallback for page 11: [page_011.jpg](page_images/page_011.jpg)


<!-- page: 12 -->

## 5. 模型的建立及求解

## 5 . 1 问题一

根据题目要求，考虑两个 BSS 互听的场景，进行相应模型的建立，采用数值分析方法求出相关参数的数值解，评估系统的吞吐。
## 5 . 1 . 1 模型建立与求解

问题 1 中考虑两个 BSS 互听的情形，并且仅考虑两个接入点分别向各自的覆盖区域内的一个站点发送数据，而不考虑站点向接入点的上行数据传输，两个 BSS 共用一个信道，基于上述具体场景建立该系统的数学模型。由于该系统中的两个 AP 在发送数据包时采用DCF 机制接入同一个信道，当仅有一个 AP 接入信道时，由于信道频谱资源充足且不会存在另一个 AP 发送的信号的干扰，接收端一定可以解调出信号，也就是数据一定能成功传输；而当出现两 AP 的退避计数器同时回退到 0 而同时接入信道时，假设两 AP 的信号强度相当，即信号的 SIR 较低，当信号经过信道传输之后在接收端进行解调时，接收端无法成功解调出目标信号，即出现丢包现象，也就是两个 AP 的数据传输都失败。通过以上分析我们可以推理出该系统的信道只可能处于三种不同的状态，即空闲（两个 AP 都不发送数据） 、发送成功（仅有一个 AP 发送数据）和发送失败（两个 AP 同时发送数据）      。我们通过将每个状态都看成一个虚拟的时隙，那么信道会在这三种虚拟时隙中不断进行转化。由于每个 AP 在不同的时隙所处的状态不同，因此我们将每个 AP 的状态看做一个随机过程，并且利用每个 AP 的退避计数器在每个时隙所处的阶数和随机回退数数值来显式表达该随机过程。在该随机过程中，我们可以知道每一时隙的状态仅与前一时隙的状态有关，因此可以用马尔科夫链来表示，通过推导该马尔科夫链的不同状态的转移概率进而得到 AP 在每个虚拟时隙的发送概率 τ 和发生碰撞的条件概率 p，从而评估该 2BSS 系统的吞吐。

该系统中 AP 的退避随机过程具体描述如下：令 x(t) 和 y(t) 表示一个 AP 的退避计数器在 t 时刻所处的退避阶数和退避计数，其中 t 是一个离散的虚拟时隙的开始时刻。用 i表示 AP 的一个数据的发送次数，即该 AP 发送的数据包与另一个 AP 发送的数据包发生了多少次的碰撞，其也叫做阶数；j 表示退避计数器的回退计数，即再经过多少个时隙之后再次发送数据；n 为最大重传次数；m 是最大退避阶数；通过采用二进制指数退避算法，那么两个 AP 的竞争窗口 CW 可用公式 (1) 表示：

{
Wi = 2 i W0 , 0 ≤ i ≤ m  
(1)
Wi = 2 m W0 , m ≤ i ≤ n  

该二维时间离散状态离散随机过程 {x(t), y(t)} 可以用二维的马尔可夫链表示，如图
### 5.1 所示。图 5.1 中，该二维马尔科夫链的每一行表示退避计数器在每个空闲时隙的开始

时刻减 1，并且转移概率为 1；每一列则代表因与其他        { () ( ) AP} 发生碰撞导致重新进入回退过程。本文中，我们选择 bi,j = limt→∞ P x t = i, y t = j 代表该二维马尔科夫链的稳态解，其中 i ∈ [0, n], j ∈ [0, Wi − 1]。

p 为某个时隙发生碰撞的概率，马尔可夫链一步状态转移概率为：

P {i, j | i, j + 1} = 1          j ∈ [0, Wi − 2], i ∈ [0, n] 1−p P {0, j | i, 0} =                j ∈ [0, W0 − 1], i ∈ [0, n − 1] W0 p                                          (2) P {i, j | i − 1, 0} =            j ∈ [0, Wi − 1], i ∈ [1, n] Wi P {0, j | n, 0} =                j ∈ [0, Wi − 1] W0

> Visual fallback for page 12: [page_012.jpg](page_images/page_012.jpg)


<!-- page: 13 -->

图 5.1   马尔可夫链模型

公式 (2) 中每个式子分别具有不同的物理意义，代表了回退过程的不同情景。第一个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，回退过程计数器在每个空闲时隙的初始时刻以概率 1 减一。当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，当该数据传输成功后，AP 缓存中的下一个数据或新获取的数据需要重新在初始竞争窗口 [0, W0 − 1] 中以相等的概率随机选择一个数值开始回退。第三个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，若在第 i − 1 次发送过程中与另外的 AP 发送的数据产生了碰撞，那么竞争窗口大小翻倍，该数据包需要重新在 [0, Wi − 1] 间以相同的概率随机选择一个数值开始回退。第四个式子表示，当该 AP 发送某一数据包的次数达到该退避机制要求的最大值时，无论本次数据发送成功还是失败，竞争窗口都会重置，并且该数据包被丢弃，开始进行下一个数据包的传输。

通过对该马尔科夫链进行分析，可知该马尔科夫链的任意状态之间可达，是不可约的。

任意状态到另一状态的步长不存在周期。从任何状态出发，都能到达另一状态，具有常返性。因此该二进制退避过程的非周期不可约马尔科夫链具有稳态解，且所有稳态的概率之和为 1。

本文中用 bi,j = limt→∞ P {x (t ) = i, y (t) = j} , i ∈ (0, n) , j ∈ (0, Wi − 1) 表示马尔可夫链的稳态解，从图 5.1 中可以看出，考虑该 AP 发送某一数据包失败一次的情景，状态bi−1,0 到状态 bi,0 的步长包括 bi−1,0 → bi,0 , bi−1,0 → bi,1 → bi,0 , · · · · · · , bi−1,0 → bi,Wi −1 →· · · · · · → bi,1 → bi,0(, 共 Wi 种，将其进行求和可得，                             ) bi,0 = bi−1,0 ∗ p ∗ W1i + p ∗ W1i ∗ 1 + . . . + p ∗ W1i ∗ 1Wi −1 = p ∗ bi−1,0 ，即

bi,0 = p ∗ bi−1,0   0<i≤n     →    bi,0 = pi ∗ b0,0   0<i≤n               (3)

> Visual fallback for page 13: [page_013.jpg](page_images/page_013.jpg)


<!-- page: 14 -->

同理，对于任意状态 bi,k ，有{
bi−1,0 ∗ p ∗ WWi −k ,                              0<i<n  
bi,k =              Wi −k i
∑                                                 (4)  
(1 − p) ∗ Wi ∗ ri=0 bi,0 ,                         i=0  

将公式 (3) 代入公式 (4)，得到，Wi − k
bi,k =          ∗ bi,0 ,            0 ≤ i ≤ n, 0 ≤ k ≤ Wi − 1            (5)  
Wi由于马尔科夫链中，到达稳态时，所有状态概率之和为 1，可得
∑  
W i −1 ∑  
r            ∑  
r             ∑  
W
Wi − k ∑  
i −1       r Wi + 1
```
                 1=              bi,k =          bi,0             =     bi,k                 (6)
                      k=0 i=0              i=0          k=0
                                                             Wi     i=0

    综合公式 (1) 和 (6) ，可得：
           {        2(1−p)(1−2p)
                 (1−2p)(1−pr+1 )W0 (1−p)(1−(2p)r+1 )
                                                           ,                          r≤m
         b00 =                             2(1−p)(1−2p)                                      (7)
                 W0 (1−(2p)m+1 )(1−p)+(1−2p)(1−pr+1 )+W0 2m pm+1 (1−pr−m )(1−2p)
                                                                                 ,    m<r

    回退计数器倒数到 0 后 AP 发送数据，因此 AP 在一个时隙发送数据的概率可以表示
为
                                      ∑
                                      r
```

## 1 − pr+1

τ=             bi,0 = b0,0 ∗                                (8)  
i=0  
1−p

当发送数据发生冲突时，另一个 AP 也在发送数据，因此条件概率 p 可表示为
```
                                    p = 1 − (1 − τ )2−1 = τ                                  (9)
    联立公式 (8) (9) 组成二元非线性方程组，运用数值分析方法求解，得 p = τ = 0.1046。
    该系统中至少有一个 AP 发送数据的概率 Ptr 可以表示为
                                          Ptr = 1 − (1 − τ )2                               (10)
    在至少有一个 AP 发送数据的条件下，恰好仅有一个 AP 发送数据的概率 Ps 为
                                         C21 ∗ τ ∗ (1 − τ )2−1
                                    Ps =                                                    (11)
                                                   Ptr
  通过以上系统建模，该系统中信道处于三种虚拟时隙的概率可由 τ 和 p 表示，空闲时
隙的长度 Te 为 slotTime，成功传输和碰撞的传输时长 Ts 和 Tc 分别表示为
                        TS = H + E[P ] + SIF S + ACK + DIF S                                (12)

                        TC = H + E[P ] + DIF S + ACKT imeout                                (13)
    该系统吞吐 S 可以表示为
                                         PS ∗ Ptr ∗ E[P ]
                 S=                                                                         (14)
                      (1 − Ptr ) ∗ Te + PS ∗ Ptr ∗ TS + (1 − PS ) ∗ Ptr ∗ TC
    带入计算得到的数值后，求得吞吐 S = 68.36M bps。
```


> Visual fallback for page 14: [page_014.jpg](page_images/page_014.jpg)


<!-- page: 15 -->

## 5 . 1 . 2 仿真实验分析

为了验证上述建立模型的准确性，我们使用 Matlab 编写仿真器，实现 CSMA/CA 机制，评估系统的吞吐。其中，仿真参数如表 1、仿真流程如图 5.2 所示。

表1     仿真参数

仿真参数      值          仿真参数       值

ACK 时长       32μs        CWmax     1024 SIFS 时长      16μs    最大重传次数         32 DIFS 时长      43μs 数据包载荷长度        1500Bytes SLOT 时长       9μs    PHY 头时长     13.6Bytes ACKTimeout 时长 65μs          物理层速率   455.8Mbps CWmin     16          RSSI    -70dBm

图 5.2   仿真流程图

> Visual fallback for page 15: [page_015.jpg](page_images/page_015.jpg)


<!-- page: 16 -->

## 5 . 1 . 3 结果分析

根据仿真器仿真数据，我们绘制了仿真时间 300 秒内的各项数据变化曲线，从而直观的评估所建立模型的准确性。

由于 CSMA/CA 中存在随机回退机制，给仿真过程带来了随机性，可以预见的是仿真过程中不同时刻系统的吞吐必然存在一定的波动。因此，我们计算仿真中系统在每秒内的吞吐量，得到如图 5.3 所示的系统吞吐随仿真时间变化的曲线。从图中可以看到，系统吞吐量一直在随时间波动，这与上述分析是吻合的。吞吐上限为 72.64Mbps，下限为 64.35Mbps，可见这种波动并不是随意的，而是限制在一定范围内，并具备一定的统计特性。通过分析这一统计特性，我们得到系统吞吐的均值为 66.87Mbps，与建模求解得到的数值结果67.17Mbps 之间的误差仅为 0.45%，有效验证了所建数学模型的合理性和正确性。

图 5.3   系统吞吐量

此外，我们还绘制了单个 AP 节点的吞吐变化情况，如图 5.4 所示。通过统计分析，我们发现两个 AP 节点的吞吐量具有近乎相同的统计特性，其最大值、最小值、平均值以及方差等特性几乎相等。

在问题一中，不存在隐藏节点且丢包率为零，但由于并发时的 SIR 较低，导致两个 AP节点的数据传输都失败。为了进一步评价模型准确性，我们还绘制了在仿真过程中并发次数占总发送次数的比例随时间变化的曲线，如图 5.5 所示分析该曲线的统计特性得到并发传输的比例为 5.1%，与建模求解得到的数值结果 5.5% 基本吻合。

综上，通过对比模型计算结果和仿真分析结果，我们不难发现所建立的模型较为合理，能够正确反映两 BSS 互听，并发 SIR 较低时系统的吞吐情况。

> Visual fallback for page 16: [page_016.jpg](page_images/page_016.jpg)


<!-- page: 17 -->

图 5.4   单 AP 吞吐量

图 5.5    系统碰撞数量与碰撞比例

## 5 . 2 问题二

根据题目要求，考虑两个 BSS 互听的场景，进行相应模型的建立，采用数值分析方法求出相关参数的数值解，评估系统的吞吐。
## 5 . 2 . 1 模型建立与求解

问题 2 中也是考虑 2 个 BSS 互听的场景，并且仅考虑两个接入点分别向各自的覆盖区域内的一个站点发送数据，而不考虑站点向接入点的上行数据传输，两个 BSS 共用一个信

> Visual fallback for page 17: [page_017.jpg](page_images/page_017.jpg)


<!-- page: 18 -->

道，基于上述具体场景建立该 2BSS 系统数学模型。本问题中系统场景与问题 1 有所不同，在该场景中假设两个 AP 同时发送数据时，接收端接受到的另一个 AP 的信号强度相比于自身关联的 AP 的信号强度而言较小，即 SIR 较大，可以成功解调出信号，因此当两 AP的退避计数器同时回退到 0 发送数据时，两 AP 的数据传输能成功。通过上述分析可以得知本系统中信道可能处于三种状态：空闲（两个 AP 都不发送数据）          、单 AP 传输（仅有一个 AP 发送数据）、并行传输（两个 AP 同时发送数据）。我们通过将每个状态都看成一个虚拟的时隙，那么信道会在这三种虚拟时隙中不断进行转化。由于每个 AP 在不同的时隙所处的状态不同，在一个特定的时隙中可以看作一个随机变量，因此我们将每个 AP 的状态看作一个随机过程，并且利用每个 AP 的退避计数器在每个时隙所处的阶数和随机回退数数值来显式表达该随机过程。但是，在本问题中，当两 AP 同时发送数据时，两者都成功，退避计数器的阶数不会改变。因此我们可以将该随机过程表示为一维随机过程，并且可以推导出每一时隙的状态仅与前一时隙的状态有关，因此可以用马尔科夫链来表示，通过推导该马尔科夫链的不同状态的转移概率进而得到 AP 在每个虚拟时隙的发送概率 τ 和发生碰撞的条件概率 p，从而评估该 2BSS 系统的吞吐。

该系统中 AP 的退避随机过程具体描述如下：令 x(t) 表示 t 时刻一个 AP 退避随机过程的退避计数，t 是一个离散的虚拟时隙的开始时刻。用 i 表示一个数据的发送次数，也叫做阶数，n 为最大重传次数，由于该系统中两 AP 不会发生碰撞，因此竞争窗口 CW 不会翻倍，用 W0 表示。

随机过程 x(t) 可以用一维的马尔科夫链表示，如图
[      ]  5.6 所示。bi = limt→∞ P {x(t) = i}  
代表一维马尔科夫链的稳态解，其中 i ∈ 0, W0 − 1 。

图 5.6      马尔可夫链模型

该马尔科夫链一步状态转移概率为：
P {i − 1 | i} = 1 i ∈ [1, W0 − 1]  
## 1 (15)

P {i | 0} =       i ∈ [0, W0 − 1]  
W0公式 (15) 中每个式子分别代表着相应的物理含义。第一个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，回退过程计数器在每个空闲时隙的初始时刻以概率 1 减一。第二个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，当该数据传输成功后，AP 缓存中的下一个数据或新获取的数据需要重新在初始竞争窗口 [0, W0 − 1] 中以相等的概率随机选择一个数值开始回退。

经过推导可以得出该马尔科夫链的任意状态之间可达，是不可约的。任意状态到另一状态的步长不存在周期。从任何状态出发，都能到达另一状态，具有常返性。因此该过程的非周期不可约马尔科夫链具有稳态解，且所有稳态的概率之和为 1。

令 bi = limt→∞ P {x (t) = i} , i ∈ (0, W0 − 1) 表示马尔科夫链的稳态解，从图 5.6中可以看出，对于不考虑碰撞的情况，状态 b0 到状态 bi 的步长包括，b0 → bi , b0 →bi+1 (→ bi , · · · · · · , b0 → bW0 −1 →
) · · · · · · → bi+1 → bi ，共 W0 − i 种，求和可得，bi =  
b0 ∗ W10 + W01∗1 + . . . + W0 ∗11W0 −i ，即，W0 − i
b i = b0 ∗             0 < i ≤ W0 -1                   (16)  
W0

> Visual fallback for page 18: [page_018.jpg](page_images/page_018.jpg)


<!-- page: 19 -->

根据马尔可夫链的性质，所有稳态的概率之和为 1，因此有，

∑  
W 0 −1          ∑  
W 0 −1

```
                         1=            bi =            b0 ∗ (W0 − i)/W0     (17)
                               i=0             i=0


     求解，可得 b0 = W02+1 = 0.117647。
     AP 退避计数器回退到 0 时发送数据，因此 AP 在一个时隙发送数据帧的概率为：τ =
b0 = 0.117647，当发生并行传输时，另外一个 AP 也传输数据，共有 2 个 AP，因此条件碰
撞概率 p 可表示为 p = 1 − (1 − τ )2−1 = τ 。
     运用数值分析法求解（牛顿法、弦截法），得 p = τ = 0.117647。
     该系统中至少有一个 AP 发送数据的概率 Ptr 可以表示为

                                       Ptr = 1 − (1 − τ )2                  (18)

   在至少有一个 AP 发送数据的条件下，恰好仅有一个 AP 发送数据的概率 Ps 为

                                          C21 ∗ τ ∗ (1 − τ )2−1
                                Ps =                                        (19)
                                                    Ptr

  通过以上系统建模，该系统中信道处于三种虚拟时隙的概率可由 τ 和 p 表示，空闲时
隙的长度 Te 为 slotTime，单 AP 传输和并行传输的时长 Ts 和 Tp 分别表示为
                            [ ]
                  TS = H + E P + SIF S + ACK + DIF S (20)

                               [ ]
                     TP = H + E P + SIF S + ACK + DIF S                     (21)

   该系统吞吐 S 可以表示为
                              (PS + 2 ∗ (1 − PS )) ∗ Ptr ∗ E[P ]
            S=                                                              (22)
                   (1 − Ptr ) ∗ Te + PS ∗ Ptr ∗ TS + (1 − PS ) ∗ Ptr ∗ TP

   带入计算得到的数值后，求得吞吐 S = 70.56M bps。
```

## 5 . 2 . 2 仿真实验分析

问题二仿真器可由问题一仿真器修改并发传输过程得到，物理层速率为 455.8Mbps，其他仿真参数同问题一，仿真流程如图 5.7。

> Visual fallback for page 19: [page_019.jpg](page_images/page_019.jpg)


<!-- page: 20 -->

图 5.7   仿真系统流程图

## 5 . 2 . 3 结果分析

根据仿真器仿真数据，我们绘制了仿真时间 300 秒内的各项数据变化曲线，从而直观的评估所建立模型的准确性。通过仿真结果的统计特性，我们得到系统吞吐的均值为68.00Mbps，与建模求解得到的数值结果 70.56Mbps 之间的误差仅为 3.6%，有效验证了所建数学模型的合理性和正确性。

虽然本题设置的物理层速率相比于问题 1 大幅降低，但是最终得到的系统吞吐量却未明显改变，原因有二：一是本题假设并发传输时 SIR 较高，两个 AP 的数据都能传输成功，系统吞吐量得以提升。二是本题不存在传输失败的情况，竞争窗口大小一直保持在CWmin，降低了随机退避过程的等待时间，从而使相同的时间内能发送更多的数据包，提升了系统的吞吐量。

> Visual fallback for page 20: [page_020.jpg](page_images/page_020.jpg)


<!-- page: 21 -->

图 5.8   系统吞吐量

图 5.9   单 AP 吞吐量

> Visual fallback for page 21: [page_021.jpg](page_images/page_021.jpg)


<!-- page: 22 -->

图 5.10   系统并发传输比例

## 5 . 3 问题三

根据题目要求，考虑两个 BSS 无法互听，并且因信道变化产生丢包的场景，进行相应模型的建立，采用数值分析方法求出相关参数的数值解，评估系统的吞吐。
## 5 . 3 . 1 模型建立与求解

问题 3 考虑 2 个 BSS 不互听的场景，具体表现为两 AP 之间距离较远，AP 间 RSSI 低于 CCA 门限，AP 一直认为信道处于空闲状态，退避计数器会一直计数，直到回退到 0 之后发送数据，并且认为当两 AP 发送数据包在时间上有交叠时，SIR 较小，导致两 AP 数据发送均失败；并且本问题中认为信道不是理想的，也就是说即使没有另外一个 AP 干扰也存在一定的几率发送失败，并且这一事件和发生碰撞是相互独立的，基于上述场景建立该 2BSS 系统数学模型。由于该系统中的两个 AP 不互听，所以有很大概率会出现二者同时或先后开始发送数据的情况，当出现这种情况时，两 AP 的数据发送均失败，由此可以推导出信道可能处于三种状态：空闲（两个 AP 都不发送数据）、发送成功（仅有一个 AP发送数据且没有丢包）和发送失败（两个 AP 同时发送数据或一个 AP 发送数据但丢包）。

我们将每个状态看作是一个虚拟时隙，那么信道将在三种虚拟时隙中转化。由于每个 AP在不同的时隙所处的状态不同，在一个特定的时隙中可以看作一个随机变量，因此我们将每个 AP 的状态看作一个随机过程，并且利用每个 AP 的退避计数器在每个时隙所处的阶数和随机回退数数值来显式表达该随机过程。在该随机过程中，我们可以知道每一时隙的状态仅与前一时隙的状态有关，因此可以用马尔科夫链来表示，通过推导该马尔科夫链的不同状态的转移概率进而得到 AP 在每个虚拟时隙的发送概率 τ 和发生交叠的条件概率 p，从而评估该 2BSS 系统的吞吐。

该系统中 AP 的退避随机过程具体描述如下：令 x(t) 和 y(t) 表示 t 时刻一个 AP 退避随机过程的退避阶数和退避计数，t 是一个离散的虚拟时隙的开始时刻。用 i 表示一个数据的发送次数，也叫作阶数，n 为最大重传次数，m 是最大退避阶数，则两个 AP 的竞争窗口 CW 可用公式 (23) 表示:

{
Wi = 2 i W0 , 0 ≤ i ≤ m  
(23)
Wi = 2 m W0 , m ≤ i ≤ n  

> Visual fallback for page 22: [page_022.jpg](page_images/page_022.jpg)


<!-- page: 23 -->

该二维时间离散状态离散随机过程 {x(t), y(t)} 可以用二维的马尔可夫链表示，如图
### 5.11 所示。 { () () }

bi,j = limt→∞ P x t = i, y t = j 代表二维马尔科夫链的稳态解，其中 i ∈ [0, n], j ∈[0, Wi − 1]。

图 5.11 二维马尔科夫链模型

p 为某个时隙发生碰撞的概率，马尔科夫链一步状态转移概率为：

P {i, j|i, j + 1} = 1                         j ∈ [0, Wi − 2], i ∈ [0, n] (1 − Pe ) ∗ (1 − p) P {0, j | i, 0} =                             j ∈ [0, W0 − 1] , i ∈ [0, n − 1] W0 [Pe + (1 − Pe ) ∗ p]                                      (24) P {i, j | i − 1, 0} =                         j ∈ [0, Wi − 1] , i ∈ [1, n] Wi P {0, j | n, 0} =                             j ∈ [0, Wi − 1] W0

公式 (24) 中每个式子分别代表着相应的物理含义。第一个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，回退过程计数器在每个空闲时隙的初始时刻以概率 1 减一。第二个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，当该数据传输成功后，AP 缓存中的下一个数据或新获取的数据需要重新在初始竞争窗口 [0, W0 − 1] 中以相等的概率随机选择一个数值开始回退。第三个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，若在第 i − 1 次发送过程中与另外的 AP 发送的数据产生了交叠或者发生了丢包，那么竞争窗口大小翻倍，该数据包需要重新在 [0, Wi − 1] 间以相同的概率随机选择一个数值开始回退。第四个式子表示，当该 AP 发送某一数据包的次数达到该退避机制要求的最

> Visual fallback for page 23: [page_023.jpg](page_images/page_023.jpg)


<!-- page: 24 -->

大值时，无论本次数据发送成功还是失败，竞争窗口都会重置，并且该数据包被丢弃，开始进行下一个数据包的传输。

通过对该进行分析可知该马尔科夫链的任意状态之间可达，是不可约的。任意状态到另一状态的步长不存在周期。从任何状态出发，都能到达另一状态，具有常返性。因此该二进制退避过程的非周期不可约马尔科夫链具有稳态解，且所有稳态的概率之和为 1。

令 p′ = Pe + (1 − Pe ) ∗ p 表示某个时隙发包失败的概率，则马尔科夫链一步状态转移概率可改写为
P {i, j | i, j + 1} = 1   j ∈ [0, Wi − 2], i ∈ [0, n]  
1−p  ′     [          ]       [     ]
P {0, j | i, 0} =         j ∈ 0, W0 − 1 , i ∈ 0, n − 1  
W0 p′                                                          (25)
P {i, j | i − 1, 0} =     j ∈ [0, Wi − 1], i ∈ [1, n]  
Wi
## 1 [ ]

P {0, j | n, 0} =          j ∈ 0, Wi − 1  
W0令 bi,j = limt→∞ P {x (t ) = i, y (t) = j} , i ∈ (0, n) , j ∈ (0, Wi − 1) 表示马尔可夫链的稳态解，从图 5.7 中可以看出，当 AP 发送失败时，状态 bi−1,0 到状态 bi,0 的步长包括，bi−1,0 → bi,0 , bi−1,0 → (bi,1 → bi,0 , · · · · · · , bi−1,0 → bi,Wi −1 → · · · · · · →) bi,1 → bi,0 , 共 Wi 种，求和可得 bi,0 = bi−1,0 ∗ p′ ∗ W1i + p′ ∗ W1i ∗ 1 + . . . + p′ ∗ W1i ∗ 1Wi −1 = p′ ∗ bi−1,0 ，即

bi,0 = p′ ∗ bi−1,0    0<i≤n             →       bi,0 = p′i ∗ b0,0   0<i≤n             (26)  
同理，对于任一状态 bi,j ，有，{
bi−1,0 ∗ p′ ∗ WWi −j ,                                0<i<n  
bi,j =          ′    Wi −j i
∑                                                          (27)  
(1 − p ) ∗ Wi ∗ ni=0 bi,0 ,                           i=0  

将公式 (26) 带入公式 (27) ，可得Wi − j
bi,j =          ∗ bi,0 ,      0 ≤ i ≤ n, 0 ≤ j ≤ Wi − 1                       (28)  
Wi马尔科夫链中，到达稳态时，所有状态概率之和为 1，可得
∑  
W i −1 ∑  
n            ∑  
n          ∑  
Wi − j ∑ Wi + 1  
W i −1 n
```
                     1=         bi,j =     bi,0           =     bi,j                                    (29)
                        j=0 i=0        i=0      j=0
                                                     Wi     i=0

     综合公式 (23) 和 (29) ，可得：
      
                     2(1 − p′ )(1 − 2p′ )
      
       (1 − 2p′ )(1 − p′n+1 )W (1 − p′ )(1 − (2p′ )n+1 ) ,                                          n≤m
b00 =
      
                                          2(1 − p′ )(1 − 2p′ )
                                                                                                  , m<n
        W0 (1 − (2p′ )m+1 )(1 − p′ ) + (1 − 2p′ )(1 − p′n+1 ) + W0 2m p′m+1 (1 − p′n−m )(1 − 2p′ )
                                                                                               (30)

     当 AP 的回退计数器倒数到 0 后则开始发送数据，AP 在一个时隙发送数据帧的概率为
                                              ∑
                                              n
```

## 1 − p′n+1

τ1 =         bi,0 = b0,0 ∗                                       (31)  
i=0  
## 1 − p′


> Visual fallback for page 24: [page_024.jpg](page_images/page_024.jpg)


<!-- page: 25 -->

隐藏节点在易受干扰时段发送数据并与源发送的数据冲突的概率 τ2 可以表示为

其中，V 是易受干扰时段的长度，表示在多长时间内隐藏节点发送的数据会对源节点发送的数据产生影响，用退避时隙的个数表示
H + E [P ] + SIF S  
V =                                                             (33)  
slotT ime X 是竞争窗口大小大于 V 的最小退避阶数。发送数据发生冲突时，隐藏节点在易受攻击时段发送数据，该系统模型中隐藏节点个数为 1，因此条件碰撞概率 p 可表示为

p = 1 − (1 − τ2 )1 = τ2                                     (34)  

因为某个时隙发包失败的概率 p′ 为(      )
p ′ = Pe + 1 − Pe ∗ p                                       (35)  

联立公式组成三元非线性方程组，运用数值分析方法求解。

tphy + Lmac header+payload /rate + SIF S   13.6 + (1500 + 30) × 8/455.8 + 16 V =                                             =                                   = 6.27 slotT ime                                   9

X=1  

                                                             ( ′ )m+1 (             )       (               )1 − 2 p∑m ∑V                1−p  ′m+1 V (V + 1)                      
```
 τ2 =             bi,j = (V + 1) ∗                     −                       ∗         ′    ∗ b0,0
        i=0 j=0
```

## 1 − p′ 2W0 1 − p2

该系统中至少有一个 AP 发送数据的概率 Ptr 可以表示为

Ptr = 1 − (1 − τ1 )2                                       (36)  

> Visual fallback for page 25: [page_025.jpg](page_images/page_025.jpg)


<!-- page: 26 -->

在至少有一个 AP 发送数据的条件下，恰好仅有一个 AP 发送数据的概率 Ps 为

C21 ∗ τ1 ∗ (1 − τ2 )1  
Ps =                                                 (37)  
Ptr

通过以上系统建模，该系统中信道处于三种虚拟时隙的概率可由 τ1 、τ2 和 p 表示，空闲时隙的长度 Te 为 slotTime，成功传输和交叠的时长 Ts 和 Ts 分别表示为[ ] TS = H + E P + SIF S + ACK + DIF S (38)

[ ]
TC = TIN _Hidden + H + E P + ACKT imeout                            (39)  

其中 TIN _Hidden 为易受干扰时段的上界，表示为

TIN _Hidden =     ∗ (H + E[P ])                         (40)  
该系统吞吐 S 可以表示为
Ps ∗ Ptr ∗ E[P ] ∗ (1 − Pe )  
S=                                                                   (41)  
(1 − Ptr ) ∗ Te + Ps ∗ Ptr ∗ Ts + (1 − Ps ) ∗ Ptr ∗ Tc

带入计算得到的数值后，求得吞吐 S = 49.1793M bps分别将附录中给定的六组参数带入所建数学模型，进行分析求解，得到系统在不同的参数下性能指标如表 2 所示。

表2    不同参数下系统性能

参数序号      CWmin        CWmax 最大重传次数 物理层速率                              吞吐量       误差参数 1       16           1024           32          455.8Mbps      49.1793Mbps   0.0457参数 2       16           1024            6          286.8Mbps      41.8185Mbps   0.0346参数 3       32           1024            5          286.8Mbps      33.9049Mbps   0.0378参数 4       16           1024           32          286.8Mbps      44.1665Mbps   0.0466参数 5       16           1024            6          158.4Mbps      34.4623Mbps   0.0494参数 6       32           1024            5          158.4Mbps      27.0140Mbps   0.0406参数 7       16           1024           32          158.4Mbps      38.0350Mbps   0.0412

## 5 . 3 . 2 仿真实验

通过对附录 6 中各种参数的仿真实验，得到了系统吞吐量随各种参数变化的情况。为了较为直观的反映所建数学模型在不同系统参数下对现实情况的逼近程度，我们绘制了计算吞吐量和仿真量之间的差值在不同系统参数下的变化情况，如图 5.12。尽管吞吐误差在不同系统参数下有所波动，但均未超过 5%，可见所建数学模型相对比较准确，但同时可以看到计算吞吐量在所有参数设置下始终大于仿真吞吐量，说明所建立的数学模型倾向于过高估计系统吞吐量。

> Visual fallback for page 26: [page_026.jpg](page_images/page_026.jpg)


<!-- page: 27 -->

图 5.12   不同参数的系统误差

图 5.13   参数 1 的系统吞吐量

图 5.13 中，参数 1 为：CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速率为 455.8Mbps。

> Visual fallback for page 27: [page_027.jpg](page_images/page_027.jpg)


<!-- page: 28 -->

(a) 参数 2 的系统吞吐量                 (b) 参数 3 的系统吞吐量

图 5.14   参数 2，3 的系统吞吐量

```
    图 5.14 中，参数 2 为：CWmin = 16，CWmax = 1024，最大重传次数为 6，物理层速
率为 286.8Mbps。参数 3 为 CWmin = 32，CWmax = 1024，最大重传次数为 5，物理层速率
为 286.8Mbps。




        (a) 参数 4 的系统吞吐量                 (b) 参数 5 的系统吞吐量

                    图 5.15   参数 4，5 的系统吞吐量

    图 5.15 中，参数 4 为：CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速
率为 286.8Mbps。参数 3 为 CWmin = 16，CWmax = 1024，最大重传次数为 6，物理层速率
为 158.4Mbps。
```


> Visual fallback for page 28: [page_028.jpg](page_images/page_028.jpg)


<!-- page: 29 -->

(a) 参数 6 的系统吞吐量                 (b) 参数 7 的系统吞吐量

图 5.16   参数 6，7 的系统吞吐量

```
  图 5.16 中，参数 6 为：CWmin = 32，CWmax = 1024，最大重传次数为 5，物理层速
率为 158.4Mbps。参数 7 为 CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速
率为 158.4Mbps。
  改变参数设置，系统的性能也会随之改变。对比图 5.13 和 5.15（a）          ，当物理层速率降
低时，系统吞吐能力略有降低。对比图 5.14（a）和 5.15（b）可见，仅调整最大重传次数
```

几乎对系统性能没有影响。对比图 5.14（a）和 5.14（b）, 增大最小竞争窗口的大小，会导致节点的平均退避时间增大，进而降低节点的发送频率，对系统的吞吐产生负面影响。
## 5 . 4 问题四

根据题目要求，考虑三个 BSS 互听的场景，进行相应模型的建立，采用数值分析方法求出相关参数的数值解，评估系统的吞吐。
## 5 . 4 . 1 模型建立与求解

问题 4 考虑 3 个 BSS 的复杂场景，在该场景中每个 AP 只向覆盖范围内的一个终端设备发送数据，三者共用同一个信道，由于 AP2 所处位置的原因该 AP 可以与另外两 AP 互听，在 DCF 机制中发送机会被另外两 AP 挤占，此外当 AP 发送数据后接收端可能同时接收到其他 AP 发送的数据而干扰信号的解调，存在隐藏节点问题，根据上述问题分析建立该 3BSS 系统的数学模型。该系统中 AP1 与 AP3 不互听，AP2 与两者都互听，AP1 只会与AP2 发生碰撞，而会与 AP3 发生交叠，由于假设 AP1 和 AP3 发包时间交叠时，SIR 较大，两者发送均成功；AP2 与两者都互听，当发生碰撞时，假设 SIR 较小，认为两者数据传输都失败。那么信道可能处于四种状态：空闲、单节点传输、并行传输和传输失败。将每个状态看作一个虚拟时隙，那么信道在四种虚拟时隙中转化。节点在每个时隙退避器所处的阶数和随机回退数状态仅与前一时隙的状态有关，因此用二维 Markov chain 来表示，进而推导节点在每个虚拟时隙的发送概率 τ 和发生碰撞的条件概率 p，从而评估该 3BSS 系统的吞吐。

该系统中 AP 的退避随机过程具体描述如下：令 x(t) 和 y(t) 表示 t 时刻一个 AP 退避随机过程的退避阶数和退避计数，t 是一个离散的虚拟时隙的开始时刻。用 i 表示一个数据的发送次数，也叫作阶数，n 为最大重传次数，m 是最大退避阶数，则两个 AP 的竞争

> Visual fallback for page 29: [page_029.jpg](page_images/page_029.jpg)


<!-- page: 30 -->

窗口 CW 可用公式 (42) 表示:

{
Wi = 2 i W0 , 0 ≤ i ≤ m  
(42)
Wi = 2 m W0 , m ≤ i ≤ n  

该二维时间离散状态离散随机过程 {x(t), y(t)} 可以用二维的马尔可夫链表示，如图
### 5.17 所示。

图 5.17     马尔可夫链模型

p1 为某个时隙发生碰撞的概率，马尔科夫链一步状态转移概率为：

P {i, j|i, j + 1} = 1                j ∈ [0, Wi − 2], i ∈ [0, n]
1−P  
P {0, j | i, 0} =                    j ∈ [0, W0 − 1] , i ∈ [0, n − 1] W0 p                                                (43) P {i, j | i − 1, 0} =                j ∈ [0, Wi − 1] , i ∈ [1, n] Wi P {0, j | n, 0} =                    j ∈ [0, Wi − 1] W0式中每个式子分别代表一定的物理含义。第一个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，回退过程计数器在每个空闲时隙的初始时刻以概率 1 减一。第二个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，当该数据传输成功后，AP 缓存中的下一个数据或新获取的数据需要重新在初始竞争窗口 [0, W0 − 1] 中以相等的概率随机选择一个数值开始回退。第三个式子表示，当该 AP 发送某一数据包的次数还没有到达该退避机制要求的重传上限时，若在第 i − 1 次发送过程中与另外的 AP 发送的数据产生了碰撞，那么竞争窗口大小翻倍，该

> Visual fallback for page 30: [page_030.jpg](page_images/page_030.jpg)


<!-- page: 31 -->

数据包需要重新在 [0, Wi − 1] 间以相同的概率随机选择一个数值开始回退。第四个式子表示，当该 AP 发送某一数据包的次数达到该退避机制要求的最大值时，无论本次数据发送成功还是失败，竞争窗口都会重置，并且该数据包被丢弃，开始进行下一个数据包的传输。

该马尔可夫链的任意状态之间可达，是不可约的。任意状态到另一状态的步长不存在周期。从任何状态出发，都能到达另一状态，具有常返性。因此该二进制退避过程的非周期不可约马尔可夫链具有稳态解，且所有稳态的概率之和为 1。

令 bi,j = limt→∞ P {x (t ) = i, y (t) = j} , i ∈ (0, n) , j ∈ (0, Wi − 1) 表示马尔可夫链的稳态解，从图 5.7 中可以看出，当 AP 发送失败时，状态 bi−1,0 到状态 bi,0 的步长包括，bi−1,0 → bi,0 , bi−1,0 → (bi,1 → bi,0 , · · · · · · , bi−1,0 → bi,Wi −1 → · · · · · · →) bi,1 → bi,0 , 共 Wi 种，求和可得 bi,0 = bi−1,0 ∗ p′ ∗ W1i + p′ ∗ W1i ∗ 1 + . . . + p′ ∗ W1i ∗ 1Wi −1 = p′ ∗ bi−1,0 ，即

bi,0 = p1 ∗ bi−1,0    0<i≤n         →       bi,0 = pi1 ∗ b0,0   0<i≤n                 (44)  

同理，对于任一状态 bi,j ，有，{
bi−1,0 ∗ p1 ∗ WWi −j ,      0<i<n  
bi,j =               Wi −j i
∑n                                                         (45)  
(1 − p1 ) ∗ Wi ∗ i=0 bi,0 , i = 0  

将公式 (44) 代入公式 (45)，得到，Wi − j
bi,j =          ∗ bi,0 ,   0 ≤ i ≤ n, 0 ≤ j ≤ Wi − 1                          (46)  
Wi

马尔科夫链中，到达稳态时，所有状态概率之和为 1，可得

∑  
W i −1 ∑  
n           ∑  
n       ∑  
Wi − j ∑ Wi + 1  
W i −1  n
```
                     1=         bi,j =     bi,0           =     bi,j                                    (47)
                        j=0 i=0        i=0      j=0
                                                     Wi     i=0

综合公式 (42) 和 (47) ，可得：
      
                     2(1 − p)(1 − 2p)
      
       (1 − 2p′ )(1 − pn+1 )W (1 − p)(1 − (2p)n+1 ) ,                                      n≤m
b00 =
      
                                       2(1 − p)(1 − 2p)
                                                                                        , m<n
        W0 (1 − (2p) m+1 )(1 − p) + (1 − 2p)(1 − pn+1 ) + W0 2m pm+1 (1 − pn−m )(1 − 2p)
                                                                                           (48)

     回退计数器倒数到 0 后发送数据，节点在一个时隙发送数据帧的概率为
                                            ∑
                                            n
```

## 1 − pn+1

τ1 =     bi,0 = b0,0 ∗                                           (49)  
i=0  
1−p

隐藏节点在易受干扰时段发送数据并与源发送的数据冲突的概率 τ2 可以表示为

> Visual fallback for page 31: [page_031.jpg](page_images/page_031.jpg)


<!-- page: 32 -->

V 是易受干扰时段的长度，X 是竞争窗口大小大于 V 的最小退避阶数。发送数据发生冲突时，隐藏节点在易受干扰时段发送数据，该系统模型中隐藏节点个数为 1，覆盖节点个数也为 1，因此条件碰撞概率 p 可表示为

p = 1 − (1 − τ1 )1 (1 − τ2 )1 = τ1 + τ2 − τ1 τ2   (51)

联立公式 (48) (49) (50) (51) 组成三元非线性方程组，通过对该非线性方程组进行分析，发现每个变量之间存在复杂的耦合关系，且难以进行分解，运用传统方法难以进行求解，因此我们通过运用牛顿迭代法，利用泰勒展开式将非线性方程近似转化为线性方程，通过设置合理的初值交替迭代使每个变量向最优值逼近直到达到终止条件。

该系统中至少有一个 AP 发送数据的概率 Pt r 可以表示为

Ptr = 1 − (1 − τ1 )2                 (52)  

在至少有一个 AP 发送数据的条件下，恰好仅有一个 AP 发送数据的概率 Ps 为
C31 ∗ τ1 ∗ (1 − τ1 )2−1 (1 − τ2 )1  
Ps =                                          (53)  
Ptr通过以上系统建模，该系统中信道处于三种虚拟时隙的概率可由 τ1 、τ2 和 p 表示，空闲时隙的长度 Te 为 slotTime，成功传输和碰撞的时长 Ts 和 Tc 分别表示为

Ts = H + E[P ] + SIF S + ACK + DIF S              (54)

## 2 1

Tc =     ∗ TC_Covered + ∗ TC_Hidden           (55)  
## 3 3

TIN _Hidden 为隐藏 AP 易受干扰时段的上界，表示为
TIN _Hidden =     ∗ (H + E[P ])            (56)  
互听 AP 之间发送数据包的碰撞时 TC_Covered 表示为

TC_Covered = TIN _Covered + H + E[P ] + ACKT imeout    (57)

互听 AP 之间发生碰撞的易受干扰时间 TIN _Covered 表示为
TIN _Covered =     ∗ slotT ime            (58)  

> Visual fallback for page 32: [page_032.jpg](page_images/page_032.jpg)


<!-- page: 33 -->

TC_Hidden = TIN _Hidden + H + E[P ] + ACKT imeout                   (59)  

该系统吞吐 S 可以表示为
PS ∗ Ptr ∗ E[P ]  
S=                                                                   (60)  
(1 − Ptr ) ∗ Te + PS ∗ Ptr ∗ TS + (1 − PS ) ∗ Ptr ∗ TC

带入计算得到的数值后，求得吞吐 S = 106.54M bps分别将附录中给定的六组参数带入所建数学模型，进行分析求解，得到系统在不同的参数下性能指标如表 3 所示。

表3    不同参数下系统性能

参数序号      CWmin        CWmax 最大重传次数 物理层速率                            吞吐量        误差参数 1       16           1024          32         455.8Mbps        106.54Mbps   0.1169参数 2       16           1024           6         286.8Mbps        98.63Mbps    0.1173参数 3       32           1024           5         286.8Mbps        72.85Mbps    0.1263参数 4       16           1024          32         286.8Mbps        98.19Mbps    0.1211参数 5       16           1024           6         158.4Mbps        84.81Mbps    0.1185参数 6       32           1024           5         158.4Mbps        64.31Mbps    0.1238参数 7       16           1024          32         158.4Mbps        84.13Mbps    0.1256

## 5 . 4 . 2 仿真实验

通过对附录 6 中各种参数的仿真实验，得到了系统吞吐量随参数变化的情况。为了较为直观的反映所建数学模型在不同系统参数下对现实情况的逼近程度，我们绘制了计算吞吐量和仿真量之间的差值在不同系统参数下的变化情况，如图 5.18。尽管吞吐误差在不同系统参数下有所波动，但总体稳定在 11%-13%，相较于前几题误差有所扩大。

图 5.18    不同参数的系统误差

> Visual fallback for page 33: [page_033.jpg](page_images/page_033.jpg)


<!-- page: 34 -->

图 5.19   参数 1 的系统吞吐量

图 5.19 中，参数 1 为：CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速率为 455.8Mbps。

(a) 参数 2 的系统吞吐量                  (b) 参数 3 的系统吞吐量

图 5.20    参数 2，3 的系统吞吐量

```
    图 5.20 中，参数 2 为：CWmin = 16，CWmax = 1024，最大重传次数为 6，物理层速
率为 286.8Mbps。参数 3 为 CWmin = 32，CWmax = 1024，最大重传次数为 5，物理层速率
为 286.8Mbps。
```


> Visual fallback for page 34: [page_034.jpg](page_images/page_034.jpg)


<!-- page: 35 -->

(a) 参数 4 的系统吞吐量                 (b) 参数 5 的系统吞吐量

图 5.21   参数 4，5 的系统吞吐量

```
    图 5.21 中，参数 4 为：CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速
率为 286.8Mbps。参数 3 为 CWmin = 16，CWmax = 1024，最大重传次数为 6，物理层速率
为 158.4Mbps。




        (a) 参数 6 的系统吞吐量                 (b) 参数 7 的系统吞吐量

                    图 5.22   参数 6，7 的系统吞吐量

  图 5.22 中，参数 6 为：CWmin = 32，CWmax = 1024，最大重传次数为 5，物理层速
率为 158.4Mbps。参数 7 为 CWmin = 16，CWmax = 1024，最大重传次数为 32，物理层速
率为 158.4Mbps。
  改变参数设置，系统的性能也会随之改变。对比图 5.19、5.21（a）和 5.22（b）          ，当物
理层速率降低时，系统吞吐能力略有降低。对比图 5.20（a）和 5.21（a）可见，仅调整最
```

大重传次数几乎对系统性能没有影响。对比图 5.20（a）和 5.20（b）, 增大最小竞争窗口的大小，会导致节点的平均退避时间增大，进而降低节点的发送频率，对系统的吞吐产生负面影响。

> Visual fallback for page 35: [page_035.jpg](page_images/page_035.jpg)


<!-- page: 36 -->

## 6. 模型评价

## 6 . 1 模型的优点

（1）本文针对 WLAN 网络信道接入机制进行研究，基于 Bianchi 等人提出的经典的马尔科夫链进行改良，推导不同场景中节点的多种状态以及在不同状态下达到稳态时各状态的转移概率，计算出系统的理论吞吐量。与仿真结果的对比表明，本文建立的模型能够有效反映系统的网络性能。

（2）模型建立过程采用数学推导的办法，可解释性强，便于人们阅读理解。
## 6 . 2 模型的缺点

（1）在复杂场景中，模型的准确性会有不同程度的降低。

（2）在模型的建立过程中做了一些假设，在实际应用中效果可能会打折扣。

> Visual fallback for page 36: [page_036.jpg](page_images/page_036.jpg)


<!-- page: 37 -->

## 参考文献

[1] Bianchi G, IEEE 802.11-saturation throughput analysis, IEEE communications letters, 2(12):

318-320, 1998.

[2] Chatzimisios P, Vitsas V, Boucouvalas A C, Throughput and delay analysis of IEEE 802.11 protocol, Proceedings 3rd IEEE International Workshop on System-on-Chip for Real-Time Applications, IEEE, 168-174, 2002.

[3] Hung F Y, Marsic I, Performance analysis of the IEEE 802.11 DCF in the presence of the hidden stations, Computer Networks, 54(15):2674-2687, 2010.

[4] Chen D R, Zhang Y J, Is dynamic backoff effective for multi-rate WLANs?, IEEE communi-cations letters, 11(8):647-649, 2007.

[5] 高峰, IEEE 802.11 无线网络 DCF 协议性能分析及提升研究, 北京邮电大学, 2010.

[6] 陈忠真, IEEE802. 11 DCF 算法的研究, 西安电子科技大学, 2012.

[7] 彭林哲, 无线局域网 DCF 性能分析与公平性改进研究, 湖南大学, 2015.

[8] 高春雪, 无线传感器网络 DCF 接入机制建模与性能分析, 燕山大学, 2012.

> Visual fallback for page 37: [page_037.jpg](page_images/page_037.jpg)


<!-- page: 38 -->

## 附录

```matlab
%% 问题一
clear;
clc;
close all;

%% Model Parameters
SIFS = 16; slot_time = 9; DIFS = 43; %us
payload_bits = 12000; mac_header_bits = 240; %bits
physical_rate = 455.8; %Mbps
payload = payload_bits / physical_rate; mac_header = mac_header_bits
   /physical_rate; %us
phy_header = 13.6; ACK = 32; ACKTimeout = 65; %us
%packet = payload + mac_header;
CWmin = 16;
m = 6;   %maxStage
r = 32; %maxRetransCnt
sim_time = 0;
throughput = [];
throughput_ap1 = [];
throughput_ap2 = [];
collision_num = [];
collision_ratio = [];

%% Simulation
fprintf('Simulating...\n');
for sim_cnt = 1:300
ap_num = 2;
suc_pkt = 0;
suc_pkt_single = [0 0];
tran_time = 0;
collision_pkt = 0;
ap_stage = zeros(1,ap_num);
ap_retrans = zeros(1,ap_num);
next_tran_time = zeros(1,ap_num);

for i = 1:ap_num % 初始化随机退避时间
ap_stage(i) = 0;
next_tran_time(i) =floor(CWmin * rand) * slot_time;
end

while tran_time < 1000000
tran_time = min(next_tran_time);
no_tran = sum(tran_time == next_tran_time);

if no_tran == 1 % 传输成功
suc_pkt = suc_pkt + 1;
for i = 1:ap_num
if next_tran_time(i) == tran_time %筛选传输节点
suc_pkt_single(i) = suc_pkt_single(i) + 1;
ap_stage(i) = 0;
ap_retrans(i) = 0;
```


> Visual fallback for page 38: [page_038.jpg](page_images/page_038.jpg)


<!-- page: 39 -->

next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + SIFS+ACK+DIFS+floor(CWmin*rand)*slot_time;if tran_time >= 1000000 sim_time = next_tran_time (i) + phy_header + mac_header + payload +SIFS + ACK + DIFS;end else next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + SIFS+ACK+DIFS;
```matlab
end
end

else % 冲突
collision_pkt = collision_pkt + 1;
for i = 1:ap_num
if next_tran_time(i) == tran_time
if ap_stage(i) < m
ap_stage(i) = ap_stage(i) + 1;
end
if ap_retrans(i) < r
ap_retrans(i) = ap_retrans(i) + 1;
else
ap_stage(i) = 0;
ap_retrans(i) = 0;
end
next_tran_time(i)=next_tran_time(i) + phy_header + mac_header +
   payload + ACKTimeout + DIFS +
   floor(2^ap_stage(i)*CWmin*rand)*slot_time;
end
end
end
end
throughput = [throughput physical_rate * suc_pkt * payload /
   sim_time];
throughput_ap1 = [throughput_ap1 physical_rate * suc_pkt_single(1) *
   payload / sim_time];
throughput_ap2 = [throughput_ap2 physical_rate * suc_pkt_single(2) *
   payload / sim_time];
collision_num = [collision_num collision_pkt];
collision_ratio = [collision_ratio collision_pkt / (collision_pkt +
   suc_pkt)];
end

%% plot total throughput
plot(1:1:300,throughput,'LineWidth',1);
title('系统吞吐量','FontSize',15,'FontName','楷体')
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem1\totalThroughput.svg']);

%% plot single ap throughput
plot(1:1:300,throughput_ap1,1:1:300,throughput_ap2,'LineWidth',1);
title('单AP吞吐量','FontSize',15,'FontName','楷体')
```


> Visual fallback for page 39: [page_039.jpg](page_images/page_039.jpg)


<!-- page: 40 -->

```matlab
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
l = legend('AP1','AP2');
set(l,'FontSize',12);
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem1\singleThroughput.svg']);

%% plot number of collision
% yyaxis left;
%
   plot(1:1:300,collision_num,'LineWidth',1,'Color',[255/255,128/255,0/255]);
% title('碰撞数据包分析','FontSize',15,'FontName','楷体')
% xlabel('时间/s','FontSize',15,'FontName','楷体');
%
% yyaxis right;
%
   plot(1:1:300,collision_ratio,'LineWidth',1,'Color',[64/255,105/255,224/255]);
% ylim([0,0.1]);
% legend('碰撞数量','碰撞比例','FontName','楷体');
% saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem1\collisionNum.svg']);
plot(1:1:300,collision_ratio,'LineWidth',1,'Color',[64/255,105/255,224/255])
title('碰撞数据包分析','FontSize',15,'FontName','楷体')
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('碰撞比例','FontSize',15,'FontName','楷体');
close all;

%% plot cdf of throughput
[h1, stats1] = cdfplot(throughput);
title('经验CDF','FontSize',15,'FontName','楷体');
xlabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
saveas(gcf, ['C:\Users\A\Desktop\数学建模\problem1\troughoutCDF.svg']);
[h2, stats2] = cdfplot(throughput_ap1);
[h3, stats3] = cdfplot(throughput_ap2);
[h4, stats4] = cdfplot(collision_ratio);


%% 问题二
clear;
clc;
close all;

%% Model Parameters
SIFS = 16; slot_time = 9; DIFS = 43; %us
payload_bits = 12000; mac_header_bits = 240; %bits
physical_rate = 275.3; %Mbps
payload = payload_bits / physical_rate; mac_header = mac_header_bits
   /physical_rate; %us
phy_header = 13.6; ACK = 32; ACKTimeout = 65; %us
%packet = payload + mac_header;
CWmin = 16;
m = 6;   %maxStage
r = 32; %maxRetransCnt
sim_time = 0;
```


> Visual fallback for page 40: [page_040.jpg](page_images/page_040.jpg)


<!-- page: 41 -->

```matlab
throughput = [];
throughput_ap1 = [];
throughput_ap2 = [];
same_num = [];
same_ratio = [];

%% Simulation
fprintf('Simulating...\n');
for sim_cnt = 1:300
ap_num = 2;
suc_pkt = 0;
same_pkt = 0;
suc_pkt_single = [0 0];
tran_time = 0;
ap_retrans = zeros(1,ap_num);
next_tran_time = zeros(1,ap_num);

for i = 1:ap_num % initial backoff time
next_tran_time(i) =floor(CWmin * rand) * slot_time;
end

while tran_time < 1000000
tran_time = min(next_tran_time);
no_tran = sum(tran_time == next_tran_time);

if no_tran == 1 % 传输成功
for i = 1:ap_num
if next_tran_time(i) == tran_time
suc_pkt_single(i) = suc_pkt_single(i) + 1;
suc_pkt = suc_pkt + 1;
if tran_time >= 1000000
sim_time = next_tran_time (i) + phy_header + mac_header + payload +
   SIFS + ACK + DIFS;
end
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS+floor(CWmin*rand)*slot_time;
else
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS;
end
end

else % 并发传输
suc_pkt = suc_pkt + 2;
same_pkt = same_pkt + 1;
if tran_time >= 1000000
sim_time = next_tran_time (i) + phy_header + mac_header + payload +
   SIFS + ACK + DIFS;
end
for i = 1:ap_num
suc_pkt_single(i) = suc_pkt_single(i) + 1;
if next_tran_time(i) == tran_time
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS + ACK + DIFS + floor(CWmin*rand)*slot_time;
```


> Visual fallback for page 41: [page_041.jpg](page_images/page_041.jpg)


<!-- page: 42 -->

```matlab
end
end
end
end
throughput = [throughput physical_rate * suc_pkt * payload /
   sim_time];
throughput_ap1 = [throughput_ap1 physical_rate * suc_pkt_single(1) *
   payload / sim_time];
throughput_ap2 = [throughput_ap2 physical_rate * suc_pkt_single(2) *
   payload / sim_time];
same_num = [same_num same_pkt];
same_ratio = [same_ratio same_pkt / (suc_pkt - same_pkt)];
end

%% plot total throughput
plot(1:1:300,throughput,'LineWidth',1);
title('系统吞吐量','FontSize',15,'FontName','楷体');
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem2\totalThroughput.svg']);

%% plot single ap throughput
plot(1:1:300,throughput_ap1,1:1:300,throughput_ap2,'LineWidth',1);
title('单AP吞吐量','FontSize',15,'FontName','楷体');
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
l = legend('AP1','AP2');
set(l,'FontSize',12);
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem2\singleThroughput.svg']);

%% plot number of collision
% yyaxis left;
%
   plot(1:1:300,same_num,'LineWidth',1,'Color',[255/255,128/255,0/255]);
% title('碰撞数据包分析','FontSize',15,'FontName','楷体')
% xlabel('时间/s','FontSize',15,'FontName','楷体');
%
% yyaxis right;
%
   plot(1:1:300,same_ratio,'LineWidth',1,'Color',[64/255,105/255,224/255]);
% ylim([0,0.1]);
% legend('碰撞数量','碰撞比例','FontName','楷体');
% saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem2\TransSameTimeNum.svg']);
% close all;
plot(1:1:300,same_ratio,'LineWidth',1,'Color',[64/255,105/255,224/255]);
title('并发传输情况','FontSize',15,'FontName','楷体');
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('并发比例','FontSize',15,'FontName','楷体');
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem2\TransSameTimeNum.svg']);
close all;
```


> Visual fallback for page 42: [page_042.jpg](page_images/page_042.jpg)


<!-- page: 43 -->

%% plot cdf of throughput [h, stats] = cdfplot(throughput);title('经验CDF','FontSize',15,'FontName','楷体');xlabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');saveas(gcf, ['C:\Users\A\Desktop\数学建模\problem2\troughoutCDF.svg']);

```matlab
%% 问题三
clear;
clc;
close all;

%% Model Parameters
SIFS = 16; slot_time = 9; DIFS = 43; %us
payload_bits = 12000; mac_header_bits = 240; %bits
physical_rate = 455.8; %Mbps
payload = payload_bits / physical_rate; mac_header = mac_header_bits
   /physical_rate; %us
phy_header = 13.6; ACK = 32; ACKTimeout = 65; %us
%packet = payload + mac_header;
CWmin = 16;
m = 6;   %maxStage
r = 32; %maxRetransCnt
pe = 0.1;
sim_time = 0;
throughput = [];

%% Simulation
fprintf('Simulating...\n');
for sim_cnt = 1:300
ap_num = 2;
suc_pkt = 0;
cross_pkt = 0;
drop_pkt = 0;
ap_retrans = zeros(1,ap_num);
ap_stage = zeros(1,ap_num);
next_tran_time = zeros(1,ap_num);

for i = 1:ap_num % 初始化
next_tran_time(i) =floor(CWmin * rand) * slot_time;
end

while suc_pkt < 10000
[tran_time,idx_min]= min(next_tran_time);
[cross_time,idx_max] = max(next_tran_time);
cross_tran = abs(diff(next_tran_time)) > (phy_header + mac_header +
   payload + SIFS);

if cross_tran == 1 % 传输成功
if(rand > 0.1)
suc_pkt = suc_pkt + 1;
for i = 1:ap_num
if next_tran_time(i) == tran_time
```


> Visual fallback for page 43: [page_043.jpg](page_images/page_043.jpg)


<!-- page: 44 -->

if suc_pkt == 10000 sim_time = next_tran_time (i) + phy_header + mac_header + payload +SIFS + ACK + DIFS;end ap_stage(i) = 0;ap_retrans(i) = 0;next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + SIFS+ACK+DIFS+floor(CWmin*rand)*slot_time;
```
end
end
else
drop_pkt = drop_pkt + 1;
if ap_stage(idx_min) < m
ap_stage(idx_min) = ap_stage(idx_min) + 1;
end
if ap_retrans(idx_min) < r
ap_retrans(idx_min) = ap_retrans(idx_min) + 1;
else
ap_stage(idx_min) = 0;
ap_retrans(idx_min) = 0;
end
for i = 1:ap_num
if next_tran_time(i) == tran_time
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + ACKTimeout + DIFS +
   floor(2^ap_stage(i)*CWmin*rand)*slot_time;
end
end
end
else % 传输重叠
cross_pkt = cross_pkt + 2;
for i = 1:ap_num
if ap_stage(i) < m
ap_stage(i) = ap_stage(i) + 1;
end
if ap_retrans(i) < r
ap_retrans(i) = ap_retrans(i) + 1;
else
ap_stage(i) = 0;
ap_retrans(i) = 0;
end
end
if abs(next_tran_time(idx_max) - next_tran_time(idx_min)) > ACKTimeout
next_tran_time(idx_min) = next_tran_time(idx_max) + phy_header +
   mac_header + payload + DIFS +
   floor(2^ap_stage(idx_min)*CWmin*rand)*slot_time;
else
next_tran_time(idx_min) = next_tran_time(idx_min) + phy_header +
   mac_header + payload + ACKTimeout + DIFS +
   floor(2^ap_stage(idx_min)*CWmin*rand)*slot_time;
end
next_tran_time(idx_max) = next_tran_time(idx_max) + phy_header +
   mac_header + payload + ACKTimeout + DIFS +
   floor(2^ap_stage(idx_max)*CWmin*rand)*slot_time;
```


> Visual fallback for page 44: [page_044.jpg](page_images/page_044.jpg)


<!-- page: 45 -->

end

```matlab
end
throughput = [throughput physical_rate * suc_pkt * payload /
   sim_time];
end

%% plot total throughput
plot(1:1:300,throughput,'LineWidth',1);
title('系统吞吐量','FontSize',15,'FontName','楷体')
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
%saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem3\totalThroughput6.svg']);
[h1, stats1] = cdfplot(throughput);


%% 问题四
clear;
clc;
close all;

%% Model Parameters
SIFS = 16; slot_time = 9; DIFS = 43; %us
payload_bits = 12000; mac_header_bits = 240; %bits
physical_rate = 455.8; %Mbps
payload = payload_bits / physical_rate; mac_header = mac_header_bits
   /physical_rate; %us
phy_header = 13.6; ACK = 32; ACKTimeout = 65; %us
%packet = payload + mac_header;
CWmin = 16;
m = 6;   %maxStage
r = 32; %maxRetransCnt
sim_time = 0;
throughput = [];
throughput_ap1 = [];
throughput_ap2 = [];
collision_num = [];
collision_ratio = [];

%% Simulation
fprintf('Simulating...\n');
for sim_cnt = 1:300
ap_num = 3;
suc_pkt = 0;
suc_pkt_single = [0 0 0];
tran_time = 0;
collision_pkt = 0;
cross_pkg = 0;
ap_stage = zeros(1,ap_num);
ap_retrans = zeros(1,ap_num);
next_tran_time = zeros(1,ap_num);

for i = 1:ap_num % 初始化
```


> Visual fallback for page 45: [page_045.jpg](page_images/page_045.jpg)


<!-- page: 46 -->

ap_stage(i) = 0;next_tran_time(i) =floor(CWmin * rand) * slot_time;end

```
while tran_time < 1000000
if(next_tran_time(2) == next_tran_time(1) && next_tran_time(1) ==
   next_tran_time(3)) %先判断三节点是否会同时传输
collision_pkt = collision_pkt + 3;
for i = 1:ap_num
if ap_stage(i) < m
ap_stage(i) = ap_stage(i) + 1;
end
if ap_retrans(i) < r
ap_retrans(i) = ap_retrans(i) + 1;
else
ap_stage(i) = 0;
ap_retrans(i) = 0;
end
next_tran_time(i)=next_tran_time(i) + phy_header + mac_header +
   payload + ACKTimeout + DIFS +
   floor(2^ap_stage(i)*CWmin*rand)*slot_time;
end
end

[tran_time,idx_min] = min(next_tran_time); %求最早传输时间

if tran_time >= 1000000
sim_time = tran_time;
end

if idx_min == 1 %AP1先开始传输
if(next_tran_time(2) == next_tran_time(1)) %判断有无并发
no_tran = 1;
else
no_tran = 0;
end
cross_tran = abs(next_tran_time(3) - next_tran_time(1)) < (phy_header
   + mac_header + payload + SIFS); %判断有无交叠
if no_tran %有并发
collision_pkt = collision_pkt + 2;
for i = 1:ap_num
if next_tran_time(i) == tran_time
if ap_stage(i) < m
ap_stage(i) = ap_stage(i) + 1;
end
if ap_retrans(i) < r
ap_retrans(i) = ap_retrans(i) + 1;
else
ap_stage(i) = 0;
ap_retrans(i) = 0;
end
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + ACKTimeout + DIFS +
   floor(2^ap_stage(i)*CWmin*rand)*slot_time;
```


> Visual fallback for page 46: [page_046.jpg](page_images/page_046.jpg)


<!-- page: 47 -->

else next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + DIFS;
```
end
end
elseif ~no_tran && ~cross_tran %无并发，无交叠
suc_pkt = suc_pkt + 1;
for i = 1:2
if next_tran_time(i) == tran_time
suc_pkt_single(i) = suc_pkt_single(i) + 1;
ap_stage(i) = 0;
ap_retrans(i) = 0;
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS+floor(2^ap_stage(i)*CWmin*rand)*slot_time;
else
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS;
end
end
elseif ~no_tran && cross_tran %无并发有交叠
cross_pkg = cross_pkg + 2;
suc_pkt = suc_pkt + 2;
next_tran_time(2) = next_tran_time(2) + floor(abs(next_tran_time(1) -
   next_tran_time(3))) + phy_header + mac_header + payload + SIFS +
   ACK + DIFS;
next_tran_time(1) = next_tran_time(1) + phy_header + mac_header +
   payload + SIFS + ACK + DIFS +
   floor(2^ap_stage(1)*CWmin*rand)*slot_time;
next_tran_time(3) = next_tran_time(3) + phy_header + mac_header +
   payload + SIFS + ACK + DIFS +
   floor(2^ap_stage(3)*CWmin*rand)*slot_time;
end
end

if idx_min == 2 %AP2先开始传输
no_tran = sum(tran_time == next_tran_time);
if no_tran == 1
suc_pkt = suc_pkt + 1;
for i = 1:ap_num
if next_tran_time(i) == tran_time
suc_pkt_single(i) = suc_pkt_single(i) + 1;
ap_stage(i) = 0;
ap_retrans(i) = 0;
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS+floor(2^ap_stage(i)*CWmin*rand)*slot_time;
else
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + SIFS+ACK+DIFS;
end
end
else
collision_pkt = collision_pkt + 2;
for i = 1:ap_num
if next_tran_time(i) == tran_time
```


> Visual fallback for page 47: [page_047.jpg](page_images/page_047.jpg)


<!-- page: 48 -->

if ap_stage(i) < m ap_stage(i) = ap_stage(i) + 1;end if ap_retrans(i) < r ap_retrans(i) = ap_retrans(i) + 1;else ap_stage(i) = 0;ap_retrans(i) = 0;end next_tran_time(i)=next_tran_time(i) + phy_header + mac_header +payload + ACKTimeout + DIFS +floor(2^ap_stage(i)*CWmin*rand)*slot_time;
```matlab
%          else
%             next_tran_time(i)=next_tran_time(i) + packet + DIFS;
else
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + DIFS;
end
end
end
end

if idx_min == 3 %%AP3先开始传输
if(next_tran_time(2) == next_tran_time(3))
no_tran = 1;
else
no_tran = 0;
end
cross_tran = abs(next_tran_time(3) - next_tran_time(1)) < (phy_header
   + mac_header + payload + SIFS);
if no_tran
collision_pkt = collision_pkt + 2;
for i = 1:ap_num
if next_tran_time(i) == tran_time
if ap_stage(i) < m
ap_stage(i) = ap_stage(i) + 1;
end
if ap_retrans(i) < r
ap_retrans(i) = ap_retrans(i) + 1;
else
ap_stage(i) = 0;
ap_retrans(i) = 0;
end
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + ACKTimeout + DIFS +
   floor(2^ap_stage(i)*CWmin*rand)*slot_time;
else
next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +
   payload + DIFS;
end
end
elseif ~no_tran && ~cross_tran
suc_pkt = suc_pkt + 1;
for i = 2:3
```


> Visual fallback for page 48: [page_048.jpg](page_images/page_048.jpg)


<!-- page: 49 -->

if next_tran_time(i) == tran_time suc_pkt_single(i) = suc_pkt_single(i) + 1;ap_stage(i) = 0;ap_retrans(i) = 0;next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + SIFS+ACK+DIFS+floor(2^ap_stage(i)*CWmin*rand)*slot_time;else next_tran_time(i) = next_tran_time(i) + phy_header + mac_header +payload + SIFS+ACK+DIFS;
```matlab
end
end
elseif ~no_tran && cross_tran
cross_pkg = cross_pkg + 2;
suc_pkt = suc_pkt + 2;
next_tran_time(2) = next_tran_time(2) + floor(abs(next_tran_time(1) -
   next_tran_time(3))) + phy_header + mac_header + payload + SIFS +
   ACK + DIFS;
next_tran_time(1) = next_tran_time(1) + phy_header + mac_header +
   payload + SIFS + ACK + DIFS +
   floor(2^ap_stage(1)*CWmin*rand)*slot_time;
next_tran_time(3) = next_tran_time(3) + phy_header + mac_header +
   payload + SIFS + ACK + DIFS +
   floor(2^ap_stage(3)*CWmin*rand)*slot_time;
end
end


end
throughput = [throughput physical_rate * suc_pkt * payload /
   sim_time];
throughput_ap1 = [throughput_ap1 physical_rate * suc_pkt_single(1) *
   payload / sim_time];
throughput_ap2 = [throughput_ap2 physical_rate * suc_pkt_single(2) *
   payload / sim_time];
collision_num = [collision_num collision_pkt];
collision_ratio = [collision_ratio collision_pkt / (collision_pkt +
   suc_pkt)];
end

%% plot total throughput
plot(1:1:300,throughput,'LineWidth',1);
title('系统吞吐量','FontSize',15,'FontName','楷体')
xlabel('时间/s','FontSize',15,'FontName','楷体');
ylabel('吞吐量/Mbps','FontSize',15,'FontName','楷体');
saveas(gcf,
   ['C:\Users\A\Desktop\数学建模\problem4\totalThroughput0.svg']);
[h1, stats1] = cdfplot(throughput);
```


> Visual fallback for page 49: [page_049.jpg](page_images/page_049.jpg)
