# 2024-E S019 论文—代码证据审计

## 身份与边界
- 论文：S019 / E24102910005 / 南京工业大学 / 60页 / train。
- 本轮：60/60 页全文文字层精读；附录第60页仅列16个文件名，未打印源码。
- 不声称原 PDF 像素级风格审查，不声称作者工程运行复现。

## Q1
路线：YOLOv10m + ByteTrack → 交通三要素 → ARIMA描述 → 基本图拟合 → 间断交通流预警 → 多元回归/AdaBoost 10分钟预测。

### 必须修复的证据问题
1. Eq.(32) `td=rhom/(rhom-rho0)*tau` 的反解应为 `tau=(rhom-rho0)/rhom*td`，正文 Eq.(33) 却除以 `rho0`。
2. D点 `rho0=165,rhom=350,td=30`：正确反解约15.86 min；按正文Eq.(33)约33.64 min；表10写25 min，三者不一致。
3. Eq.(39) 将表7首行 A/B/C 密度 192.4/226.3/227.0 代入得到约21940 veh/km，而目标为310.9，系数量级/小数点/逆变换明显有问题。
4. 134个一分钟样本按0.7/0.3切分，但正文没有说明时序切分还是随机切分。
5. 拥堵时刻在正文/表8/表9出现约1–2分钟漂移，应使用统一事件registry。

## Q2
优点：`K(t)` 将流量差积分成区间累计车辆比例，并用0.8开/0.6关形成滞回结构。
风险：Q0/Qmax、0.4/0.6权重和下游条件的校准证据不足。

## Q3
论文把开放应急车道后的 `q_D` 人工乘1.3，得到堵塞时间降低48.3%。这只能作为“容量提升30%的情景模拟”，不是观测干预或估计的因果效果。必须对1.3做敏感性并明确证据等级。

## Q4
B→E、可选F和热图利用率的思路能补充下游状态/关闭依据，但没有坐标决策变量、成本/覆盖目标、优化程序和消融验证，因此是启发式布点建议，不是已求解的布点优化。

## 可迁移规则
- 连续编号公式做代数回放。
- 最终回归式至少回放一行真实表格数据。
- 时序切分声明泛化单位。
- scenario multiplier 与 treatment effect 严格分层。
- 传感器布点必须输出可回放的数量/坐标/成本/覆盖。
