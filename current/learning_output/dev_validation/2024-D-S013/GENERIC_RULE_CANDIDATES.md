# 待审通用规则候选：未写入正式Core

本文件属于Dev调优隔离区。只提炼通用合同；不把论文特定参数、地名和解法加入Train检索。

- 变量时间支撑必须由文件选择器和聚合代码证明：单日文件不能因字段改名变成年均值。对应p85及官方数据对照。
- 空间表连接声明唯一键、连接基数和前后行数；静态维度扩年后不得忽略年份连接。对应p98的2→60反例。
- 窗口单位由独立实体时间轴定义，而非DataFrame行数；先断言entity-time唯一。对应p96的三行/三年反例。
- 评价输出记录fit实体、predict实体、target来源和transform拟合范围；写了split不代表实际使用了split。对应p92/p108。
- 数学名称必须映射到执行primitive：熵权、等权、缩放方向不能互换；公式符号的样本维和特征维要做极限/均匀分布检查。
- 自构指数的阈值标签只允许评价对指数的拟合，禁止声称外部灾害校准；必须单独登记真实标签来源。
- 空间分布椭圆的几何面积不等于资源面积；概率覆盖必须声明维数、尺度及是否外部覆盖验证。不能套用一维正态68%规则。

合并门槛：检查现有Core是否已有同义规则，优先补充最小反例而不是新增重复规则；补齐S013审计后执行隔离保护与历史兼容验证。当前没有正式规则更新或Core能力增益声明。
