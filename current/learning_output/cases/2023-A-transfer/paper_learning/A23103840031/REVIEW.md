# A23103840031 全文学习记录

2026-09-22。《WLAN网络信道接入机制建模》，厦门大学。55页可用转换文本及附录全部阅读。页码按raw_layout。未解析PDF、未检查图像像素、未完整执行作者Python程序；奖项等级未核验。

## 方法与表达中值得保留的部分

作者区分单时隙尝试概率和脆弱期内发送概率；将噪声与碰撞按条件概率合并；Q4区分中心与外侧节点。验证不只展示吞吐，还展示尝试、碰撞及分布。代码采用AP类、配置和主循环分离，便于建立参数身份记录。这些比追求单个漂亮吞吐数值更值得学习。

## Q1（第10–16页，代码第39–41页）

理论67.1744，仿真66.8309，作者相对误差0.5158%。有限退避链可以沿用为解耦近似。第14页失败时长将DIFS写成SIFS，而代码配置使用DIFS；本地主解显式统一两者。RMSE公式转换不清，不因缺失根号或平方的转换外观单独判错。

第41页标为Q1的主循环使用task2的275.3参数构造有效发送速率，但config时长为455.8；其中valid*rate仍恰好等于12000，不能声称该乘积多计载荷。真正需要检查的是场景名和计时参数来源。主循环按is_finish累计还可能混入最终丢弃，成功与完成必须分别记录。

## Q2（第18–20页）

作者τ满足τ=(τ+1)/(7.5+τ+1)，本地复算τ=0.131043674、吞吐72.6290，与正文一致。该式把系统忙事件数带入节点尝试概率；无失败固定窗口的单节点更新应τ=2/17。正文Ps=0又与全部成功设定不符。作者仿真77.7124不能用来替换本地联合链68.948748。

代码的range(0,15)产生0至14，漏掉合法值15；range(1,CW)则排除合法零值。隔离支持集检查已保存。这会改变等待时间，但未执行作者长仿真，不能把全部吞吐差距归因于这一项。

## Q3（第21–28页，附录第46页）

正确关系是pf=0.1+0.9τ2，其中τ2表示脆弱期碰撞概率。表7-1的七组pf却与τ2相同；代回后残差约−0.07039至−0.08037，不符合正文合并关系。τ1表值又全部接近无失败的2/(W+1)。附录直接令p1=0，支持该不一致判断。

附录几何级数括号及窗口增长与正文有限封顶模型存在差别。正文floor与代码ceil的脆弱期计数也不同。作者基础理论52.7934、仿真51.7832，只能记录为作者报告，不能说方程和仿真彼此已验证。两种失败事件正确分账的思想应迁移，具体数值不升级。

AP状态的channel只在time%DIFS==0更新，不能直接代表连续空闲DIFS；退避中途还可能继续递减。完成时仅查看当前信道会忘记先前交叠。隔离事件样例：源帧[0,41)、干扰帧[0,20)，完成时信道已经空闲，但源帧仍应失败。CHECKS.json保存该语义反例，属于手工构造的分支检查，不是完整作者轨迹。隐藏节点还会受ACK标志冻结，需明确这属于何种监听关系。savep虽更新，却未随savemat保存，图中碰撞率不能仅凭当前输出文件独立复算。

## Q4（第30–36页，附录第50–55页）

正文p_outer=τ_center、p_center=1−(1−τ_outer)^2体现非对称拓扑，是有用的低阶近似。用封顶有限求和恢复该闭合，基础τ_outer=0.106730087、τ_center=0.089276623；这只是该方程数值解。

第32页将两节点Ptr与含两侧成功包数的分子组合成Ps。规范化闭合七组代入得到Ps约1.470至1.476，进而Ptr(1−Ps)<0。成功包数期望可以超过1，概率不能；它们不可在时间分母中混用。不能因为理论108.4228接近仿真107.7990就忽略此问题。附录级数尾项还有括号变化，本检查未静默采用该变化，也不是逐字完整代码复现。

主循环timeMax=100000、步长1微秒对应0.1秒；Q4的百万步是1秒。运行次数和单次时长应分别报告，不能将点数当秒数。全局固定网格DIFS、无碰撞记忆等问题需由状态机解决。

## 图表与落地

吞吐随时间图用于稳定性，分布图用于波动，尝试/碰撞图用于机制诊断；三种用途不可互相替代。只检查了文本与图注证据，未验证图像样式。采纳类与配置分离、条件失败合并、节点分类及多指标验证；参考答案新增“概率与包数分别检查、碰撞标记持续到本次尝试结束”的要求。实际标量与隔离检查见[CHECKS.json](CHECKS.json)，执行入口为[complete_remaining_checks.py](../complete_remaining_checks.py)。
