> 历史阶段记录：本篇已完成89页全文文本评审，当前结论见REVIEW.md。

# A23102890028 部分评审：Q4修正系数的标定与迁移

2026-09-22。PARTIAL_MARKDOWN_REVIEW。已读物理页1–3、33–39、43–46、74、81–86全文文本；其余页仅短预览或未读。89页尚未全文完成。两份Markdown哈希已核对，没有解析PDF、查看原图或运行作者完整仿真。奖项未独立核验。

## 值得吸收的方法

作者面对难以直接解析的多节点重叠，采用基础解析式乘修正系数k，并明确在基础参数上拟合后迁移到其他参数（p36、38–39）。作者也承认固定k对窗口32的配置效果差。可迁移的学习点是：经验修正可以作为候选，但须保存标定数据身份、固定后的参数、独立验证配置及失效范围。

## 标定点不能充当独立验证

p38用基础配置的1000次蒙特卡洛模拟估计k=1.76855，再报告解析值与该模拟均值均为116.54 Mbps。这是标定点的拟合一致，不是独立验证。没有宣称作者隐瞒拟合；正文对此做了说明。改进应体现在明确区分报告里的标定行和迁移行。

代码p85–86打印np.min、np.max及mean，因此表中的区间是模拟样本极值范围，不是95%置信区间。原始完整1000个样本未获得，附录有省略号；不能由极值反推出可靠标准误差。

## 参数身份检查先于精度比较

p38表6-4及p39表6-6将三组速率写为268.8，而表6-5写286.8；p85可见模拟配置也为286.8。不能擅自决定全部行都是同一种速率。保留冲突标记，将这三行排除出严格同参数迁移误差汇总。

在无速率冲突的158.4 Mbps三组，固定系数对应的印刷预测相对作者模拟误差依次为-5.917096%、+12.053512%、-5.937299%，平均绝对相对误差7.969302%。这些是对作者表格的复算，不是新仿真精度结果。基础拟合点不纳入此均值。

若仅用表中比值倒算各组需要的系数，三组约为1.87978、1.57831、1.88018。这只是固定系数迁移失败的诊断，不把逐组重新拟合后的结果当作验证通过。窗口和重传次数共同改变，不能据此唯一归因。

## 可见公式与表值不一致

局部重建p43–46求根及p74调用，保留原可见指数、脆弱窗口取整、Tc定义和固定k。七组结果均满足概率范围及方程残差检查，基础配置得到115.633011 Mbps，而表中为116.54；其余组也有差异，详见CALIBRATION_CHECKS.json。对有速率冲突的三组另算268.8分支，未能消除全部差异。

这说明当前可见附录公式不能直接复现印刷表；并不证明作者实际采用了哪个版本。没有通过重新调k强行匹配表值，没有把局部重建称为作者原程序执行。

p35给出的权重(1/4,1/2,1/4)和节点数(2,3,2)、(1,0,1)，加权结果应为nC=2.5、nH=0.5；转换文本p36显示2/3和7/3，数值不一致。代码求失败率使用指数1.8、0.8，成功概率使用1.5、0.5，进一步表明文本/代码版本或定义需要确认。原图未看，不推断差异一定发生在原论文而非转换环节。

## 计时静态发现

p85三节点模拟返回all_bits/max(t1,t2)，遗漏t3。局部输入(100,110,200)可使这个分母与三时钟最大值不同。但这些变量含未来退避期限，即使改成max(t1,t2,t3)也不自动变成正确的已完成观察时间。本轮只保存分支反例，不宣称该输入已在实际运行中出现，也不声称这一行解释了全部吞吐偏差。后续读完整调度代码再决定是否需要确定性轨迹实验。

## 落地与下一步

新增revision_v7/CALIBRATION_SUPPLEMENT.md和知识卡，要求经验系数的拟合点、验证点分离，参数身份不符的行单独列出，极值区间不冒充置信区间。当前主解不采用第六篇k作修正，Core仍v1.41，全文评审数仍5/10。

接下来阅读p4–32、40–42、47–73、75–80、87–89，补齐Q1–Q3和两版Q4模拟代码。已有检查无需重跑。原Markdown可继续使用，无需用户补文件。
