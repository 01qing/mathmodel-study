# A23104860166 全文学习记录

2026-09-22。《基于Markov链的WLAN网络信道接入机制建模》，武汉大学。68页可用转换文本及附录已全部阅读；页码按raw_layout。无PDF解析、无原图像检查、无作者完整MATLAB执行；奖项未另核实。

## 建模与写作优点

概率矩阵、事件时序图和吞吐更新公式之间对应清晰，尤其把“情形—概率—占用时间—交付载荷”作为共同结构。Q2明确固定窗口和并发双成功；Q3把噪声限定在非碰撞分支，避免重复扣除；Q4采用缓冲状态统一更新的思想，能减少节点遍历顺序偏差。七组参数表提供中间概率，比只有最终结果更容易核验。

## Q1：解析近似与参数身份（第11–18、46–49页）

理论67.1743，摘要仿真65.2437而正文65.2780，须保留不同口径而不擅自合并。附录理论片段速率仍为286.8，而Q1基础为455.8；选择τ≈0.10462并不能保证后续吞吐使用正确速率。仿真保持逐节点退避与重传，这是可取之处。有限时域内按计划发送事件先计数可能越过观测终点，应明确完成边界；本次未量化这种有限边界影响。

## Q2：公式正确不保证附录正确（第19–23、50页）

正文τ=2/17、独立近似70.5585正确，仿真约68.9442（摘要68.9498）与本地联合链68.948748相近。成功收益包括双发两包，值得保留。

附录`tt=2/W0+1`字面为1.125，正确括号版本为0.117647；前者不能是概率。理论片段又保留286.8速率，仿真片段275.3正确。CHECKS.json复算规范化独立近似70.558464，验证的是正文路线，未替作者修好全部MATLAB后宣称复现。

## Q3：互斥分支与脆弱期（第24–32、53–59页）

令τ1为尝试、τ2为脆弱期碰撞，则成功τ1(1−τ2)(1−Pe)、碰撞τ1τ2、无碰撞噪声失败τ1(1−τ2)Pe、空闲1−τ1构成互斥完备分支。两对称节点期望收益相加不要求独立；“互相不监听”却不等于碰撞反馈独立。

第30页把V写为ceil(Ts/9)=7。基础数据时长对应5格，完整成功交换对应15格，都不是7；参数表基础V=5又与数据脆弱期相符。表中τ1/τ2与部分事件概率也不能直接互相推出，应记录参数与行号。

基础理论52.6231、仿真54.7553；其余六组理论44.2267、39.8915、44.2191、33.8528、30.9206、33.9910。附录外层455.8、辅助函数286.8，尚不能据该代码重现同一场景。没有把这套不一致参数输出作为主答案。

第57–59页事件区间以严格end<start判断分离，端点相接会进入碰撞分支。隔离样例[0,41)、[41,82)无正长度重叠，按本题声明的半开区间约定不应碰撞。该检查只证明边界语义不同，不推断长时间吞吐差值。成对推进可能遗漏其他事件的风险还须实际轨迹证明，本次不声称已证实全部此类遗漏。

## Q4：分类收益与联合时间（第33–43、60–68页）

外侧/中心分别采用齐次N=2/N=3闭合，未处理真正的联合退避状态。重叠时长取1.5Ts隐含起发偏移均匀假设，需推导或测量验证。正文四个成功情形中，s1是中心单发、s2是外侧同时双发、s3外侧单发、s4外侧错时重叠；式7.20/7.31收益写2s1+s2+s3+2s4，把前两项包数交换。正确事件收益是s1+2s2+s3+2s4。附录重组后的收益反而使用了正确单双包含义。局部检查按正文打印概率保存两种收益，不将任何一个直接称为整篇仿真复现。

基础理论110.415、仿真111.018不证明节点分配正确。158.4/W16/r6一列中心尝试率0.0537与其他W16列约0.0934差异需解释。附录外层与辅助函数W/r不同，覆盖的tau1_0并非实际返回的tau1数组，且标量乘方用于数组；现成代码不是可直接运行的单一实验。

缓冲state_next统一提交是值得迁移的代码结构。第67页唤醒用if/elseif：若两侧都暂停，仅唤醒一侧。隔离输入两侧时间均2*Tmax会显示这一分支差异；未证明该状态在完整程序中的可达频率，因此不报告它对吞吐的影响。

## 迁移与验证边界

采纳事件概率矩阵、时序图、条件噪声分支、双包奖励、缓冲同步更新；新增边界相接、代码括号和参数传递检查。图形证据仅核对文本与图注，不评价原图视觉质量。[CHECKS.json](CHECKS.json)由[complete_remaining_checks.py](../complete_remaining_checks.py)实际生成，覆盖标量与人工事件边界；作者端到端复现0篇。
