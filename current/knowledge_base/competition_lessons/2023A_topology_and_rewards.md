# 2023-A：不确定拓扑与并发奖励

状态：本地学习候选，不自动并入正式 Train，不代表新 Core 发布或跨题能力提升。

来源 A23100070049 第 9、23、31–34、42–53 页；完整证据在 learning_output/cases/2023-A-transfer/paper_learning/A23100070049/。

1. 题目只给出一部分关系时，将未知关系显式参数化，在预算允许的范围内枚举情形；不要悄悄把一个假设当成题目条件。本次保持互听图，枚举相邻边的四种对称干扰配置，补足条件化解答。新题若存在单向关系，应重新定义枚举空间。
2. 概率、事件时长和奖励分别建模。两个任务可以同时成功时，“至少一个成功”与“成功数量”不同。本次 Q2 分子漏项探针得到 62.25747，修正奖励的独立近似为 70.55846，含关联的链为 68.94875 Mbps；修正一个错误不等于全部模型精确。
3. 利用交换对称性验证映射与输出，不把有限随机样本要求为完全相同。本次混合拓扑由 AP1/AP3 交换对应，均值相差 0.048 Mbps。置信区间重叠只是诊断，不是等价性证明。
4. 多资源阻塞不能由任一释放事件无条件解除。维护阻塞者集合，在最后一个阻塞解除后才恢复；用于调度、网络、并行任务时均须重核各自语义。
5. 完整状态转移检查优先于与论文数值对齐。事件区间、同时事件、冻结时隙、成功复位、超时和计数分母均应对应题目假设。

已运行的四情形实验和短轨迹核查可直接复用，不要只因换 Chat 就重跑。作者 MATLAB 未执行、Q3 完整求解式未恢复；不把论文原图或数值作为真值。
